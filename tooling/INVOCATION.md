# A-Society Tooling Invocation (Retired)

This operator-facing tooling reference is retired.

`runtime/INVOCATION.md` is now the sole default operator-facing executable reference. The `tooling/` directory remains only as a transitional legacy implementation location when an approved migration flow explicitly scopes it.
