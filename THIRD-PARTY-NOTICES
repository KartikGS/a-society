THIRD-PARTY NOTICES
A-Society

A-Society is distributed as source. It does not bundle, vendor, or redistribute
any third-party code: node_modules/ and build outputs (dist/) are excluded from
the repository, and runtime dependencies are fetched directly from their package
registries by the operator's own `npm install`.

Because no third-party copies are redistributed here, no per-package attribution
or NOTICE reproduction is required in this repository. Each dependency remains
under its own license, delivered with the package at install time.

If A-Society is ever distributed as a bundled or published artifact (an npm
package with bundled dependencies, a committed build output, a container image,
or a release binary), attribution for the bundled components becomes required
and this file must be regenerated to list them.
