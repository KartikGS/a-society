# A-Society Tooling Development (Retired)

This workflow is retired as a permanent standing workflow.

Use `$A_SOCIETY_WORKFLOW_EXECUTABLE_DEV` for executable-layer work. The old standalone tooling workflow has been absorbed into the unified executable workflow, and the remaining framework-service code now lives under `runtime/`.
