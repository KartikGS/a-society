# A-Society Tooling Development (Retired)

This workflow is retired as a permanent standing workflow.

Use `$A_SOCIETY_WORKFLOW_EXECUTABLE_DEV` for executable-layer work. The old standalone tooling workflow has been absorbed into the unified executable workflow, with `runtime/` as the standing executable root and `tooling/` retained only as a transitional legacy implementation location when explicitly scoped.
