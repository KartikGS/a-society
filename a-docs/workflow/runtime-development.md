# A-Society Runtime Development (Retired)

This workflow is retired as a permanent standing workflow.

Use `$A_SOCIETY_WORKFLOW_EXECUTABLE_DEV` for executable-layer work. Runtime orchestration now lives inside the unified executable workflow alongside deterministic framework services, with `runtime/` as the standing executable root.
