# Role: A-Society Tooling Developer Agent (Retired)

This role is retired as a standing executable role.

Use `$A_SOCIETY_FRAMEWORK_SERVICES_DEVELOPER_ROLE` for deterministic executable framework services and `$A_SOCIETY_ORCHESTRATION_DEVELOPER_ROLE` for runtime orchestration and operator-surface ownership.

This file remains only as a historical redirect for older records and references. It is no longer indexed, loaded from `required-readings.yaml`, or used for new flow design.
