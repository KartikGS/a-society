---

**Subject:** Owner role and workflow instruction — complexity analysis references (2 changes)
**Status:** BRIEFED
**Date:** 2026-03-16

---

## Agreed Change

Two navigational gaps surfaced in the backward pass of `20260316-workflow-dynamic-complexity` and synthesized in `06-curator-synthesis.md`. Both are maintenance additions — no new content, no direction decisions.

**Change 1:** Add the complexity analysis responsibility to both Owner role documents (`$A_SOCIETY_OWNER_ROLE` and `$GENERAL_OWNER_ROLE`). Complexity analysis at intake is now a core Owner responsibility; neither role document reflects it or points to `$INSTRUCTION_WORKFLOW_COMPLEXITY`. An Owner agent reading their role document has no path to the new capability.

**Change 2:** Add a pointer to `$INSTRUCTION_WORKFLOW_COMPLEXITY` in `$INSTRUCTION_WORKFLOW` (`general/instructions/workflow/main.md`). The primary entry point for workflow guidance has no path to the intake routing instruction. A one-line addition to the "Modifying an Existing Workflow" section, or a parallel new section, resolves it.

---

## Scope

**In scope:**

- `$A_SOCIETY_OWNER_ROLE` (`a-society/a-docs/roles/owner.md`) — expand the "Workflow routing" bullet in "Authority & Responsibilities" to name complexity analysis at intake as part of the responsibility, with a pointer to `$INSTRUCTION_WORKFLOW_COMPLEXITY`
- `$GENERAL_OWNER_ROLE` (`a-society/general/roles/owner.md`) — same addition in the equivalent section
- `$INSTRUCTION_WORKFLOW` (`a-society/general/instructions/workflow/main.md`) — add a pointer to `$INSTRUCTION_WORKFLOW_COMPLEXITY` near the "Modifying an Existing Workflow" section (line 266); the exact form — added sentence, parallel section, or extended paragraph — is Curator's judgment

**Out of scope:**

- Changes to `$INSTRUCTION_WORKFLOW_COMPLEXITY` itself
- Changes to any other role documents
- Changes to the Owner role's broader responsibilities beyond the complexity analysis addition

---

## Likely Target

- `a-society/a-docs/roles/owner.md` — "Authority & Responsibilities" section, "Workflow routing" bullet
- `a-society/general/roles/owner.md` — equivalent section
- `a-society/general/instructions/workflow/main.md` — near line 266, "Modifying an Existing Workflow" section

---

## Open Questions for the Curator

None. The placement is identified, the content is a pointer addition only, and the form of each change is within Curator execution scope. No judgment calls require Owner input.

---

## Curator Confirmation Required

Before beginning Phase 1, the Curator must acknowledge this briefing in the session:

> "Briefing acknowledged. Beginning proposal for Owner role and workflow instruction — complexity analysis references."

The Curator does not begin drafting until they have read this brief in full and confirmed acknowledgment.
