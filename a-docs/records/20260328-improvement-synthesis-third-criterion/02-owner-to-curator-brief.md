**Subject:** Improvement synthesis — third merge criterion
**Status:** BRIEFED
**Date:** 2026-03-28

---

## Agreed Change

**Files Changed:**
| Target | Action |
|---|---|
| `$GENERAL_IMPROVEMENT` | modify — Synthesis Phase merge assessment criteria clause [Requires Owner approval] |
| `$A_SOCIETY_IMPROVEMENT` | modify — Synthesis Phase merge assessment criteria clause [Curator authority — implement directly] |

The merge assessment in both improvement documents' Synthesis Phase currently enumerates two criteria for identifying a merge candidate in Next Priorities:

> scan existing Next Priorities items for same target files/design area and compatible authority level

A third criterion — same workflow type and role path — was established in the doc-maintenance-bundle flow (2026-03-24) and added to `$A_SOCIETY_OWNER_ROLE` (Brief-Writing Quality section) and `$INSTRUCTION_LOG` (Owner responsibilities). The two improvement documents were not updated at that time and remain out of sync with those sources.

**Fix:** Update the merge assessment criteria clause in the Synthesis Phase section of both files to enumerate all three criteria, using the numbered-list form already established in `$A_SOCIETY_OWNER_ROLE`:

**Exact target clause (current, identical in both files):**
> scan existing Next Priorities items for same target files/design area and compatible authority level

**Replacement:**
> scan existing Next Priorities items for (1) same target files or same design area, (2) compatible authority level, and (3) same workflow type and role path

The surrounding sentence structure in both files is preserved; only the criteria clause changes.

**`[LIB]` — Update report obligation:** Because this flow modifies `$GENERAL_IMPROVEMENT`, include the framework update report draft as a named section in the proposal submission. The Curator determines classification by consulting `$A_SOCIETY_UPDATES_PROTOCOL` post-implementation; do not pre-specify classification in the proposal.

---

## Scope

**In scope:** The merge assessment criteria clause in the Synthesis Phase section of `$GENERAL_IMPROVEMENT` and `$A_SOCIETY_IMPROVEMENT`. No other changes to either file.

**Out of scope:** Any changes to `$A_SOCIETY_OWNER_ROLE`, `$INSTRUCTION_LOG`, or any other file — both are already correct and serve as the source of truth for this fix.

---

## Likely Target

- `$GENERAL_IMPROVEMENT` → `a-society/general/improvement/main.md`, Synthesis Phase, Step 2 — second bullet (Changes outside `a-docs/`), the sentence beginning "**Before filing**, apply the merge assessment:"
- `$A_SOCIETY_IMPROVEMENT` → `a-society/a-docs/improvement/main.md`, Synthesis Phase, Step 1 — second bullet (Changes outside `a-docs/`), the sentence beginning "**Before filing**, apply the merge assessment:"

---

## Open Questions for the Curator

None. The change is fully specified: the exact target clause, its location in both files, and the replacement text are all given above. The proposal round is mechanical — no judgment calls are required.

---

## Curator Confirmation Required

Before beginning Phase 1, the Curator must acknowledge this briefing in the session:

> "Briefing acknowledged. Beginning proposal for Improvement synthesis — third merge criterion."

The Curator does not begin drafting until they have read this brief in full and confirmed acknowledgment.
