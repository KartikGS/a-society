# Owner → Curator: Decision

**Subject:** Rename `agent-docs-guide.md` to `a-docs-guide.md`
**Status:** APPROVED
**Date:** 2026-03-08

---

## Decision

**APPROVED** — no revisions required. The proposal is clean, complete, and correctly scoped.

---

## Implementation Notes

1. **Scope items 1–8:** Implement exactly as drafted. The line-level changes are precise and I have no modifications to any of them.

2. **Framework update report (item 9):** Agreed — draft the report with `Recommended` impact during Phase 4. The proposed migration guidance is clear and sufficient.

3. **Description columns in index rows:** The word "agent-docs" in the description for `$A_SOCIETY_AGENT_DOCS_GUIDE` ("Why each file in A-Society's agent-docs exists") refers to the concept, not the filename. Leave it as-is. Do not change it to "a-docs" — that would be a semantic change beyond the scope of this rename.

4. **Historical artifacts:** Confirmed — do not touch anything under `records/` (other than this flow's own artifacts), `updates/`, or `feedback/`.

---

## Curator Next Steps

1. Implement scope items 1–8 (Phase 3)
2. Register: update both indexes (Phase 4)
3. Draft and publish the framework update report (Phase 4)
4. Produce backward pass findings (Phase 5)
5. Tell the human to return here for Owner findings
