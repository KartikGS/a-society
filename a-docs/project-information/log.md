# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` | Small — single-session maintenance change (one to two files) |
| `[M]` | Medium — multi-step change within a single phase |
| `[L]` | Large — multi-phase or multi-file strategic change |
| `[LIB]` | Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` | Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` | Direction decision involved; requires Owner-level scope |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v22.1.

**Recent Focus**

`[M][ADR]` — **runtime-tool-calling** (2026-03-28): Tier 3 Runtime Dev flow closed. File tool calling capability added to A-Society's runtime layer: *(1)* `runtime/src/tools/file-executor.ts` (new) — `FileToolExecutor` class with `read_file`, `write_file`, `list_directory`; workspace-root sandboxing via `path.resolve` + `startsWith` check; error results returned to model (not thrown); *(2)* `runtime/src/llm.ts` — `LLMGateway` constructor accepts optional `workspaceRoot`; iterative `MAX_TOOL_ROUNDS = 10` tool loop; tool calls executed by `FileToolExecutor`, results appended as `tool_result` messages before next provider call; *(3)* `runtime/src/providers/anthropic.ts` + `openai-compatible.ts` — updated `executeTurn` signature (`tools?: ToolDefinition[]`, returns `ProviderTurnResult`); non-streaming branch for tool-enabled turns; native format translation for both SDKs; *(4)* `runtime/src/types.ts` — new types: `ToolDefinition`, `ToolCall` (with `parseError?: string`), `ProviderTurnResult` discriminated union; `RuntimeMessageParam` extended to four variants (`user`, `assistant`, `assistant_tool_calls`, `tool_result`); `LLMProvider.executeTurn` updated; *(5)* `orient.ts`, `orchestrator.ts` — `workspaceRoot` threaded to `LLMGateway` constructor. Remediation cycle: `parseError?: string` typed field added to `ToolCall` replacing implicit `_parseError` sentinel; `PROVIDER_MALFORMED` throw added on unexpected tool_calls in no-tool path. No registration action (internal `runtime/src/` changes only). No framework update report (no `general/` changes). Backward pass initiated (2026-03-28). Record: `a-society/a-docs/records/20260328-runtime-tool-calling/`.

**Previous**

`[M][ADR]` — **runtime-provider-agnostic** (2026-03-28): Tier 3 Runtime Dev flow closed. LLM Gateway refactored from hardwired Anthropic to provider-agnostic delegating model: *(1)* `LLMProvider` interface and `RuntimeMessageParam` type added to `runtime/src/types.ts`; `LLMGatewayError` relocated there (from `llm.ts`) to avoid circular imports; *(2)* `runtime/src/llm.ts` refactored — `LLMGateway` holds `private provider: LLMProvider`, constructed via `createProvider(process.env.LLM_PROVIDER ?? 'anthropic')` factory; unknown-provider guard throws at construction time; backward-compatible no-arg constructor preserved; *(3)* `runtime/src/providers/anthropic.ts` (new) — extracted from original `llm.ts`; behavioral equivalence preserved including exact error messages; `ANTHROPIC_MODEL` env var override added; *(4)* `runtime/src/providers/openai-compatible.ts` (new) — single `OpenAICompatibleProvider` covering HuggingFace Inference, Google Gemini (via OpenAI-compat endpoint), and any OpenAI-compatible endpoint; configured via `OPENAI_COMPAT_BASE_URL` (required, construction-time guard), `OPENAI_COMPAT_API_KEY`, `OPENAI_COMPAT_MODEL`; *(5)* `runtime/src/orient.ts` — `MessageParam` renamed to `RuntimeMessageParam` (three locations); *(6)* `runtime/package.json` — `openai: ^4.0.0` added. No remediation cycle (zero deviations in TA integration assessment). No framework update report (no `general/` changes; runtime-only). Backward pass complete (Curator synthesis, 2026-03-28). Synthesis MAINT: `$A_SOCIETY_WORKFLOW_RUNTIME_DEV` Registration Phase updated with actual runtime indexing convention; `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` Advisory Standards extended with dependency selection and uncertainty-surface rules; `$A_SOCIETY_RECORDS` updated with artifact-sequence verification note. Record: `a-society/a-docs/records/20260328-runtime-provider-agnostic/`.

`[L][ADR]` — **runtime-orient-session** (2026-03-27): Runtime Dev flow closed. User-facing CLI entry point added to A-Society's runtime: *(1)* `orient` command on `runtime/src/cli.ts` — distinct `OrientSession` type (ephemeral, no SessionStore), `buildContextBundle` extended with `mode: 'flow' | 'orient'` parameter (orient mode suppresses handoff directive, injects greeting), readline conversation loop against existing `LLMGateway`; registry guard exits with spec-required error if `roleKey` absent from `roleContextRegistry`; *(2)* `runtime/bin/a-society.ts` + `runtime/bin/a-society` (new) — project discovery at `process.cwd()` via `a-docs/agents.md` signal, `@inquirer/prompts` select menu, registry key derivation (`{folderName}__Owner`), direct `runOrientSession` call; *(3)* `a-society/install.sh` (new) — `npm install` + `npm link`; *(4)* `$A_SOCIETY_INSTALL_SCRIPT` registered in `$A_SOCIETY_PUBLIC_INDEX`. Remediation cycle: registry guard missing from `orient.ts` (unregistered projects proceeded with empty context — missed by integration test due to mock API key short-circuit), `llm.ts` `MessageParam` re-export not in spec (benign; spec updated), literal `\n` escape and unused `contextHash` (quality). No framework update report (additive runtime capability; no `general/` changes; no adopting project a-docs impact). Backward pass complete (Curator synthesis, 2026-03-27). Synthesis MAINT: `$A_SOCIETY_STRUCTURE` updated to five-folder structure (tooling/ and runtime/ sections added, heading updated); `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` Advisory Standards extended with three rules (§8 completeness, type import source, extension-before-bypass). Record: `a-society/a-docs/records/20260327-runtime-orient-session/`.

`[L][ADR]` — **runtime-orchestrator-mvp** (2026-03-27): Tier 3 Runtime Dev flow closed. First implementation of A-Society's programmatic runtime layer — TypeScript/Node.js application replacing the human's manual session-switching loop: *(1)* Seven runtime components in `a-society/runtime/src/`: Operator Interface (CLI: `start-flow`, `resume-flow`, `flow-status`), Flow Orchestrator (workflow edge routing, `human-collaborative` pause/resume, terminal detection), Context Injection Service (`roleContextRegistry`-based, SHA-256 context hash), Session Store (file-backed, `runtime/.state/`), LLM Gateway (Anthropic SDK, streaming, four-class error model), Handoff Interpreter (strict `start_new` | `resume` schema, non-repairing), Tool Trigger Engine (in-process invocations of Components 1, 3, 4, 7 at declared trigger points); *(2)* `human-collaborative: "yes"` added to Owner gate nodes in `$A_SOCIETY_WORKFLOW_FRAMEWORK_DEV`, `$A_SOCIETY_WORKFLOW_TOOLING_DEV`, and `$A_SOCIETY_WORKFLOW_RUNTIME_DEV`; *(3)* `$A_SOCIETY_RUNTIME_INVOCATION` (new): `a-society/runtime/INVOCATION.md` registered in `$A_SOCIETY_PUBLIC_INDEX`. Remediation cycle resolved: four critical gaps (Tool Trigger Engine stubs, missing edge routing, `human-collaborative` not implemented, context bundle double-injection) and D-1 deviation (`start`/`start_new` normalization). No framework update report (no `general/` changes). Backward pass complete (Curator synthesis, 2026-03-27). Record: `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/`.

---

## Next Priorities

`[S][LIB][MAINT]` — **Merge assessment: third criterion missing from Synthesis Phase** — `$GENERAL_IMPROVEMENT` and `$A_SOCIETY_IMPROVEMENT` Synthesis Phase sections reference only two merge assessment criteria ("same target files/design area" and "compatible authority level") — the third criterion ("same workflow type and role path") added to `$INSTRUCTION_LOG` and `$A_SOCIETY_OWNER_ROLE` in doc-maintenance-bundle is not yet reflected there. Fix: update both Synthesis Phase merge assessment references to include the third criterion. Source: `a-society/a-docs/records/20260324-doc-maintenance-bundle/04-curator-to-owner.md` (Out-of-Scope Drift Flagged).

`[S][LIB]` — **Backward pass sequence filing: verify folder state before selecting sequence number** — `$GENERAL_IMPROVEMENT` Meta-Analysis Phase output location section contains no instruction to verify the record folder's current contents before selecting `NN` for a new findings artifact. The implicit assumption — that backward pass findings follow immediately after forward pass artifacts — breaks in any flow where the registration phase files one or more artifacts before the backward pass begins. In this flow, `07-owner-to-curator.md` and `08-curator-to-owner.md` were filed during registration before the backward pass started, shifting the actual next available number to 09; the Runtime Developer filed at 06 based on forward-pass position. Fix: add one sentence to `$GENERAL_IMPROVEMENT` Meta-Analysis Phase Step 2: "Before filing, read the record folder's current contents to identify the actual next available sequence number." Generalizable: any adopting project using records faces the same collision risk. A-Society a-docs/ component (`$A_SOCIETY_RECORDS`) implemented in 20260328-runtime-provider-agnostic synthesis. Follows Framework Dev flow (LIB proposal required). Merge assessment: existing "Merge assessment: third criterion" targets `$GENERAL_IMPROVEMENT` Synthesis Phase — different section; no merge. New item. Source: `a-society/a-docs/records/20260328-runtime-provider-agnostic/10-ta-findings.md` (Workflow Friction 1); `a-society/a-docs/records/20260328-runtime-provider-agnostic/11-owner-findings.md` (Workflow Friction 1).

`[S][LIB][MAINT]` — **Role guidance quality: shared list search, Expected response scope, `[LIB]` flow clarity, registration scope, and technical-summary discipline** — Eight `general/roles/` improvements bundled into one Framework Dev flow: *(1)* `$GENERAL_OWNER_ROLE` Brief-Writing Quality: new "Shared list constructs" paragraph — when a brief adds to an ordered criteria or conditions list, enumerate all documents containing that list before finalizing scope; *(2)* `$GENERAL_CURATOR_ROLE` Handoff Output: clarifying rule — `Expected response` names only the immediate next artifact from the receiving role; artifacts produced only after intermediate steps by another role are not in scope for this handoff; *(3)* `$GENERAL_OWNER_ROLE` Post-Confirmation Protocol: clarify that `[LIB]` registration obligations are represented within existing workflow phases, not by adding extra `workflow.md` path nodes; *(4)* `$GENERAL_OWNER_ROLE` Brief-Writing Quality: for `[LIB]` flows likely to qualify for a framework update report, the brief must explicitly instruct the Curator to include the update report draft as a named section in the proposal submission; *(5)* `$GENERAL_CURATOR_ROLE` Registration scope: clarify that registration means indexing existing documentation in the appropriate registries; authoring documentation for executable layers (e.g., `tooling/INVOCATION.md`, `runtime/INVOCATION.md`) is outside registration scope and is a Developer deliverable; *(6)* `$GENERAL_CURATOR_ROLE` implementation guidance: when summarizing technical implementations, use exact type names, method signatures, and methodology terms from approved source artifacts rather than generic substitutes; *(7)* `$GENERAL_OWNER_ROLE` TA advisory review: add a data-extraction type coverage check so parsed-model-output types include an explicit parse-failure state and internal no-tool/no-op/fallback paths are specified beyond the happy path; *(8)* `$GENERAL_OWNER_ROLE` tooling invocation discipline: when invoking framework tooling, consult the documented invocation rather than reconstructing calls from memory. Source: `a-society/a-docs/records/20260324-doc-maintenance-bundle/07-curator-findings.md` (Finding 1); `a-society/a-docs/records/20260324-doc-maintenance-bundle/08-owner-findings.md` (Findings 1 and 2); `a-society/a-docs/records/20260325-machine-readable-handoff/07-owner-findings.md` (Findings 1 and 2); `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/18-curator-findings.md` (Finding 1); `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/20-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260328-runtime-tool-calling/12-curator-findings.md` (Finding 2); `a-society/a-docs/records/20260328-runtime-tool-calling/15-owner-findings.md` (Findings 1, 2, and 4).

`[S][LIB]` — **Machine-readable handoff: phase-closure `artifact_path` semantics + completion examples** — `$INSTRUCTION_MACHINE_READABLE_HANDOFF` needs guidance for phase-closure handoffs where the receiving role verifies completion rather than reading a new proposal/decision artifact. Add worked examples covering completion/registration handoffs and clarify what `artifact_path` should point to in that pattern; assess whether synthesis/flow-closure needs a distinct documented convention as well. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/06-curator-findings.md` (Findings 2 and 3); `a-society/a-docs/records/20260325-machine-readable-handoff/07-owner-findings.md` (Finding 3).

`[M]` — **Machine-readable handoff validator (Component 8)** — New tooling component: extracts the `handoff` fenced block from agent pause-point output, parses the YAML, and validates field types, enum constraints, and the `prompt` conditional against `session_action`. Structurally analogous to Component 7 (Plan Artifact Validator) — same deterministic, rule-derived pattern, different input source. Follows standard Tooling Dev flow. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/03-curator-to-owner.md` (Tooling Validator Assessment).

`[S]` — **Runtime `synthesisRole` parameterization** — `orderWithPromptsFromFile` in `runtime/src/triggers.ts` hardcodes `'Curator'` as the synthesis role for the TERMINAL_FORWARD_PASS trigger. Correct for all current A-Society flows, but not portable to non-A-Society projects or future flows with a different synthesis role. Fix: derive `synthesisRole` from runtime configuration or workflow document rather than hardcoding. Follows Runtime Dev workflow. Source: `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/15-ta-assessment.md` BP-1.

`[S][MAINT]` — **Runtime layer a-docs/ dependency map** — Create a formal tracking artifact for the runtime layer's co-maintenance dependencies on `a-docs/` artifacts, analogous to `$A_SOCIETY_TOOLING_COUPLING_MAP`. Three dependencies identified in Phase 0 design: *(1)* machine-readable handoff schema (`$INSTRUCTION_MACHINE_READABLE_HANDOFF`) — Handoff Interpreter parses it; *(2)* supported-role context sets (roleContextRegistry) — must be updated when a supported role's required-reading list changes; *(3)* permanent workflow graph and record-folder `workflow.md` schemas — runtime orchestration assumptions and trigger wiring depend on both. Follow-on to runtime implementation; create after runtime ships. Source: `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/03-ta-to-owner.md` §1 (Flag to Owner).

`[S][MAINT]` — **Add Component 7 to `$A_SOCIETY_TOOLING_INVOCATION`** — `tooling/INVOCATION.md` documents Components 1–6 but omits Component 7 (`plan-artifact-validator.ts`). Entry point: `validatePlanArtifact(recordFolderPath: string)`. Type B coupling-map gap; should have shipped with Component 7. Merge assessment: no existing Next Priorities item targets INVOCATION.md. New item. Source: `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/15-runtime-developer-findings.md` (Finding 1); `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/19-ta-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/20-owner-findings.md` (Finding 4).

`[S][MAINT]` — **Update `$A_SOCIETY_RUNTIME_INVOCATION` with `orient` command and provider configuration** — `runtime/INVOCATION.md` has two documentation gaps: *(1)* `orient` command omitted — entry point `tsx src/cli.ts orient <workspaceRoot> <roleKey>` added in runtime-orient-session; *(2)* provider configuration undocumented — `LLM_PROVIDER` selector (`anthropic` | `openai-compatible`, default `anthropic`), `ANTHROPIC_MODEL` override, and `OPENAI_COMPAT_*` variables (`OPENAI_COMPAT_BASE_URL` required, `OPENAI_COMPAT_API_KEY` optional, `OPENAI_COMPAT_MODEL` optional) added in runtime-provider-agnostic; example configurations for HuggingFace and Gemini. Developer deliverable (authoring invocation documentation for executable layers is outside Curator registration scope). Follows Runtime Dev workflow. Merge assessment: both gaps target `runtime/INVOCATION.md` via Runtime Dev workflow — same file, compatible authority, same workflow type. Merged. Source: `a-society/a-docs/records/20260327-runtime-orient-session/10-owner-to-curator.md`; `a-society/a-docs/records/20260328-runtime-provider-agnostic/07-owner-to-curator.md`.

`[M]` — **Runtime integration test infrastructure** — The runtime layer has no test harness or format standard for integration validation. The integration gate requires a reproducible test record; the Developer improvised a manual terminal transcript that could not exercise the registry guard path (mock API key short-circuited before that path ran). A structured test harness would have surfaced the missing guard at validation time rather than in the backward pass. Follows Runtime Dev workflow. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2).

`[S][LIB]` — **Generalizable advisory standards: §8 completeness, extension-before-bypass, dependency selection, and typed extraction errors** — Four findings from runtime-orient-session, runtime-provider-agnostic, and runtime-tool-calling backward passes are explicitly generalizable across project types: *(1)* behavioral requirements in advisory prose that are not mirrored in the files-changed table are not binding implementation requirements — any project with advisory-document-producing roles faces the same structural gap; *(2)* before proposing new infrastructure or a bypass, enumerate explicitly why the existing path cannot be extended — applicable to any design or architecture role; *(3)* the extension-before-bypass standard applies to dependency/library selection as well as code architecture — before proposing per-case library implementations (separate SDKs per provider, per-format parser, per-service client), enumerate whether a common library interface covers multiple cases; *(4)* when a type represents data parsed from model output, the design should include a typed unparseable state rather than hiding parse failure inside sentinel payload keys. All four are implemented in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` Advisory Standards for A-Society; the `general/` contribution (a general instruction or role-template addition for advisory-document-producing roles) remains open. Follows Framework Dev flow (LIB proposal required). Merge assessment: existing "Role guidance quality" `[S][LIB][MAINT]` targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE` — different design area; no merge. Source: `a-society/a-docs/records/20260327-runtime-orient-session/14-ta-findings.md` (Findings 1, 3); `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Findings 1, 4); `a-society/a-docs/records/20260328-runtime-provider-agnostic/10-ta-findings.md` (Scope Concerns 1); `a-society/a-docs/records/20260328-runtime-provider-agnostic/11-owner-findings.md` (Workflow Friction 2); `a-society/a-docs/records/20260328-runtime-tool-calling/13-runtime-developer-findings.md` (Finding 4.1); `a-society/a-docs/records/20260328-runtime-tool-calling/14-ta-findings.md` (Finding 5); `a-society/a-docs/records/20260328-runtime-tool-calling/15-owner-findings.md` (Generalizable Finding 1).

`[S]` — **Provider catch blocks must preserve classified gateway errors** — `runtime/src/providers/anthropic.ts` and `runtime/src/providers/openai-compatible.ts` re-wrap already-classified `LLMGatewayError`s thrown inside provider `try` blocks as `UNKNOWN` in their outer catch blocks. Fix: add `if (err instanceof LLMGatewayError) throw err;` before SDK-specific error mapping so `PROVIDER_MALFORMED` and other classified errors retain their original code and message. Follows Runtime Dev workflow. Merge assessment: existing runtime items target synthesis-role configuration, invocation docs, or test infrastructure — different files/design area; no merge. Source: `a-society/a-docs/records/20260328-runtime-tool-calling/15-owner-findings.md` (Finding 3).

`[M][LIB]` — **Developer completion report and integration test record format** — `$INSTRUCTION_DEVELOPMENT` (`general/instructions/development/main.md`) needs two improvements: *(1)* completion report template — add an explicit "Incomplete or stubbed implementations" check; the current form asks only about spec deviations, which structurally permits stubs to be filed as "no deviation" when no deviation from the spec was made — it cannot surface absent implementations; *(2)* integration test record format — require reproducible evidence (command output, state file excerpts, error traces) for gate-preceding validation; narrative assertion without reproducibility requirements enables structurally overconfident self-certification. Both findings are generalizable to any project using Developer completion reports and self-certified integration gates. Follows Framework Dev flow (LIB proposal required). Source: `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/19-ta-findings.md` (Findings 1 and 2).

---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
