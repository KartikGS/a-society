# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` | Small — single-session maintenance change (one to two files) |
| `[M]` | Medium — multi-step change within a single phase |
| `[L]` | Large — multi-phase or multi-file strategic change |
| `[LIB]` | Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` | Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` | Direction decision involved; requires Owner-level scope |
| `[RUNTIME]` | Runtime layer change; follows Runtime Dev workflow |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v27.0.

**Recent Focus**

`[M][LIB][RUNTIME]` — **c7-removal-c3-extension-synthesis-hardcode** (2026-04-01): Multi-domain flow (Tooling Dev + Runtime Dev parallel tracks) closed. *(A) Tooling Developer:* `tooling/src/workflow-graph-validator.ts` — `validateWorkflowFile` extended with optional `strict?: boolean`; three structural invariant checks added: neighboring same-role nodes (unconditional), Owner-at-start and Owner-at-end (strict mode only); start/end nodes identified by incoming/outgoing edge absence. `tooling/src/plan-artifact-validator.ts` and `tooling/test/plan-artifact-validator.test.ts` deleted (Component 7 removed). Version-comparator test fixtures updated to v27.0 (pre-existing drift resolved). *(B) Runtime Developer:* `runtime/src/triggers.ts` — `ACTIVE_ARTIFACT` handler and `validatePlanArtifact` import removed; Component 3 invocation updated to `strict: true`; synthesis role hardcoded to `'Curator'`. `runtime/src/orchestrator.ts` — `ACTIVE_ARTIFACT` trigger call removed (type safety). `runtime/.env.sample` — `SYNTHESIS_ROLE` block and now-empty section header removed. *(C) Curator:* `tooling/INVOCATION.md`, `runtime/INVOCATION.md`, `$A_SOCIETY_ARCHITECTURE`, `$A_SOCIETY_TOOLING_COUPLING_MAP`, `$A_SOCIETY_TOOLING_PROPOSAL`, `$A_SOCIETY_WORKFLOW_TOOLING_DEV`, `$A_SOCIETY_PUBLIC_INDEX` updated for Component 7 removal and Component 3 extension. `$INSTRUCTION_WORKFLOW_COMPLEXITY` — dangling Component 7 reference removed (Curator-authority maintenance; no framework update report). Record: `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/`.

**Previous**

`[S][RUNTIME]` — **orient-startup-simplification** (2026-04-01): Runtime Dev flow closed. `runtime/src/orient.ts` — initial message updated to direct the Owner to read the project log and give a brief status before asking what the user wants to work on; startup console log removed; close message updated from "Orient session closed." to "Session closed." `runtime/INVOCATION.md` — `## Interactive Entry Point` section added documenting the `a-society` binary as the recommended entry point for interactive Owner sessions; `orient` command description updated to classify it as the underlying low-level mechanism. No `general/` changes; no framework update report. Record: `a-society/a-docs/records/20260401-orient-startup-simplification/`.

`[M][LIB][RUNTIME]` — **session-routing-removal** (2026-03-31): Multi-domain flow (two parallel tracks) closed. *(A) Framework Dev — Curator:* Session routing instructions removed from 15 active governance docs across `a-docs/` and `general/`: all workflow files (`main.md`, `framework-development.md`, `tooling-development.md`, `multi-domain-development.md`), all role files (`owner.md`, `curator.md`, `tooling-developer.md`, `runtime-developer.md`, `technical-architect.md`), general role templates (`general/roles/owner.md`, `general/roles/curator.md`), and core instructions (`$INSTRUCTION_ROLES`, `$INSTRUCTION_WORKFLOW` main.md, `$INSTRUCTION_MACHINE_READABLE_HANDOFF`, `$A_SOCIETY_COMM_HANDOFF_PROTOCOL`). Removed: Session Routing Rules section, human-as-orchestrator framing, new-session format blocks, session-start prompt clauses, and `session_action`/`prompt` schema fields and field definitions. *(B) Runtime Dev:* `runtime/src/handoff.ts` — `session_action` and `prompt` removed from `HandoffBlock` interface; all validation logic for these fields removed from `HandoffInterpreter.parse()`; return value simplified to `role` + `artifact_path`. Framework update report published (Breaking × 3 + Recommended × 1, v26.0 → v27.0). Record: `a-society/a-docs/records/20260331-session-routing-removal/`.

`[S][RUNTIME]` — **registry-frontmatter-reader** (2026-03-30): Runtime Dev flow closed. `runtime/src/registry.ts` — static `roleContextRegistry` replaced with `buildRoleContext(roleKey, projectRoot)` that reads `universal_required_reading` from `agents.md` and `required_reading` from the role file via YAML frontmatter parsing (`js-yaml`), using `resolveVariableFromIndex()`; `RoleContextEntry.namespace` field removed; `roleKeyToIndexVariable` private helper added (derives `$NAMESPACE_ROLENAME_ROLE` from roleKey); null returned for all hard failures, universal-only list returned for absent `required_reading`. `runtime/src/injection.ts` and `runtime/src/orient.ts` — callers updated to `buildRoleContext`. `runtime/src/paths.ts` — regex bug fixed (`[\$A_Z_0-9]` → `\$[\w-]+`), enabling resolution of variables with letters B–Y. No `general/` changes; no framework update report. Backward pass complete (Curator synthesis, 2026-03-30). Synthesis MAINT: `$A_SOCIETY_RECORDS` now requires the Owner intake node to appear first in `workflow.md`; `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` now requires verification of declared-operational dependency correctness and explicit operator-facing log ownership for shared error-propagation paths. New Next Priorities: role-file required-reading authority model. Record: `a-society/a-docs/records/20260330-registry-frontmatter-reader/`.

---

## Next Priorities

`[M][LIB][ADR]` — **Role-file required-reading authority model** — Evaluate how role documents should serve both runtime parsing and human/manual orientation without maintaining two authoritative required-reading lists. Scope: decide whether YAML `required_reading` / `universal_required_reading` becomes the sole authority with prose deferring to it, or whether another structure better separates machine-readable and natural-language consumers; propagate the decision across A-Society role docs and the corresponding `general/` role templates as needed. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets role-file required-reading structure, YAML/prose authority, or the runtime/manual dual-consumer role-doc design area; no merge. Source: `a-society/a-docs/records/20260330-registry-frontmatter-reader/09-curator-findings.md` (Top Finding 1; Redundant Information); `a-society/a-docs/records/20260330-registry-frontmatter-reader/10-runtime-developer-findings.md` (Top Finding 2; Redundant Information); `a-society/a-docs/records/20260330-registry-frontmatter-reader/12-owner-findings.md` (Top Finding 3; Redundant Information).

`[S][LIB]` — **Technical Architect advisory completeness addendum** — Add two generalizable TA advisory standards to `$GENERAL_TA_ROLE`: *(1)* require directory-scoped verification before asserting that a file exists or does not exist; *(2)* require advisories to name any newly required imports explicitly when specified behavior depends on them. Both were implemented directly in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`; the reusable counterpart remains open for `$GENERAL_TA_ROLE`. Follows Framework Dev workflow. Merge assessment: existing "Role guidance precision bundle" targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`; compatible authority level and workflow type, but different target file and TA-advisory completeness design area, so no merge. Updated by synthesis from `20260329-workflow-schema-unification-multi-domain` because the new finding targets the same file (`$GENERAL_TA_ROLE`), the same TA-advisory completeness design area, compatible `[LIB]` authority, and the same Framework Dev workflow path. Source: `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 1); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/10-runtime-developer-findings.md` (Top Findings 3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/11-ta-findings.md` (Top Findings 2-3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/12-owner-findings.md` (Top Findings 4).

`[M]` — **Component 4 parallel-flow correctness bundle** — Update the Backward Pass Orderer so backward traversal is derived from the workflow graph rather than flat node-list first occurrence when a flow contains parallel forks, preserving concurrent backward-pass steps per `$GENERAL_IMPROVEMENT`. In the same flow, update Component 4 prompt generation so backward-pass prompts direct agents to inspect the record folder for the next available sequence number instead of predicting filenames. Follows Tooling Dev workflow. Merge assessment: merged synthesis findings from `20260401-c7-removal-c3-extension-synthesis-hardcode` into the existing item because the new finding targets the same component and the same prompt-generation subproblem: predicted filenames caused a concrete `08-*` collision after a missing earlier backward-pass artifact. The two findings remain bundled because they target the same component, require the same Tooling Dev workflow path, and both correct cases where Component 4 output diverges from the improvement protocol. Source: `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/09-owner-forward-pass-closure.md` (Component 4 invocation override note); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/11-curator-findings.md` (Top Findings 2, 4); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/12-owner-findings.md` (Top Findings 1, 3); `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/09-owner-findings.md` (Top Finding 1; Workflow Friction).

`[S]` — **Tooling version-comparator hermeticity** — Decouple `tooling/test/version-comparator.test.ts` and its fixture set from the live framework `VERSION.md` so unrelated framework version bumps do not break tooling validation. Scope: choose a deterministic test boundary (test-local version fixture or equivalent pre-test alignment), update the affected tests/fixtures, and remove the need for developers to repair stale version fixtures during otherwise unrelated implementation flows. Follows Tooling Dev workflow. Merge assessment: no existing Next Priorities item targets `tooling/test/version-comparator.test.ts`, the version-comparator fixtures, or tooling-test hermeticity; no merge. Source: `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/08-tooling-developer-findings.md` (Top Finding 1; Missing Information; Top Finding 3; Workflow Friction).

`[M]` — **Machine-readable handoff validator (Component 8)** — New tooling component: extracts the `handoff` fenced block from agent pause-point output, parses the YAML, and validates field types against the current schema (`role` string, `artifact_path` string or null). Structurally analogous to Component 7 (Plan Artifact Validator) — same deterministic, rule-derived pattern, different input source. Note: the `session_action`/`prompt` fields and Conditional Constraint that were part of the original scope have been removed from the schema by the `session-routing-removal` flow (2026-03-31); the validator spec should reflect the simplified two-field schema only. Follows standard Tooling Dev flow. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/03-curator-to-owner.md` (Tooling Validator Assessment).


`[M]` — **Runtime integration test infrastructure** — The runtime layer has no test harness or format standard for integration validation. The integration gate requires a reproducible test record; the Developer improvised a manual terminal transcript that could not exercise the registry guard path (mock API key short-circuited before that path ran). A structured test harness would have surfaced the missing guard at validation time rather than in the backward pass. Follows Runtime Dev workflow. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2).

`[M][LIB]` — **Role guidance precision bundle: brief-state-claim verification, implementation portability gap, dependency-scoping completeness, session-routing-removal additions, structured documentation replacement boundary** — Eight role-file improvements targeting `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`. *(1)* Add a "verify current file state before making specific state claims" obligation to the Brief-Writing Quality section of `$GENERAL_OWNER_ROLE` — mirrors the existing Review Artifact Quality re-read rule; prevents asserting a specific passage exists without reading the target file. *(2)* Add an implementation-stage portability check to `$GENERAL_CURATOR_ROLE` Implementation Practices — when adapting text from a project-specific source for a general template (or vice versa), verify variable references, terminology, and examples are valid in the target context. *(3)* Add a schema-code coupling check to `$GENERAL_OWNER_ROLE` Brief-Writing Quality — when a documentation change defines or modifies a schema with a programmatic consumer, the brief must scope both the documentation and code change in the same flow. *(4)* Add removal-of-dependents scoping to `$GENERAL_OWNER_ROLE` Brief-Writing Quality — when a brief removes an item from a structured list, all dependent content (vestigial format blocks, cross-references, gated prose) must be explicitly enumerated for removal across all target files. *(5)* Add a multi-track path portability check to `$GENERAL_OWNER_ROLE` Forward Pass Closure Discipline — for flows with parallel tracks, closure must verify that all track convergence artifacts contain no machine-specific absolute paths or `file://` URLs. *(6)* Add a structured-entry replacement-boundary rule to `$GENERAL_OWNER_ROLE` Brief-Writing Quality — when directing a change within a structured documentation entry (a block with discrete sub-fields, e.g. a CLI command entry with separate description, usage, and arguments), the brief must explicitly state whether the replacement is description-only or applies to the whole entry; bare "Replace with: [new text]" scoped to a sub-field without naming the boundary is ambiguous. *(7)* Add removed-type-surface consumer enumeration to `$GENERAL_OWNER_ROLE` Brief-Writing Quality — when a brief removes or renames a union variant, enum value, interface member, event type, or similar consumed program element, it must enumerate not only the definition site but also the consuming call sites that must change in the same layer. *(8)* Add public-index variable retirement reference sweeps to `$GENERAL_OWNER_ROLE` brief/decision scoping guidance — when a public-index variable or publicly registered artifact is removed, the Owner must sweep for `$VARIABLE_NAME` references across `a-society/` and explicitly scope dependent `general/` files for upfront authorization. Items 1–2 implemented directly in `$A_SOCIETY_OWNER_ROLE` and `$A_SOCIETY_CURATOR_ROLE` in prior flows; items 3–5 implemented directly in `$A_SOCIETY_OWNER_ROLE` by synthesis of `20260331-session-routing-removal`; items 7–8 implemented directly in `$A_SOCIETY_OWNER_ROLE` by synthesis of `20260401-c7-removal-c3-extension-synthesis-hardcode`. Item 6 general-layer counterpart remains open; items 7–8 are now added to the same general-layer bundle. Follows Framework Dev workflow. Merge assessment: merged synthesis findings from `20260401-c7-removal-c3-extension-synthesis-hardcode` into the existing bundle because the new findings target the same file (`$GENERAL_OWNER_ROLE`), the same brief-writing precision design area, compatible `[LIB]` authority, and the same Framework Dev workflow path. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 1); `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/07-owner-findings.md` (Top Findings 1, 2); `a-society/a-docs/records/20260331-session-routing-removal/10-owner-findings.md` (Top Findings 1–3); `a-society/a-docs/records/20260401-orient-startup-simplification/05-runtime-developer-findings.md` (Top Finding 1; Unclear Instructions); `a-society/a-docs/records/20260401-orient-startup-simplification/06-owner-findings.md` (Top Finding 1; Unclear Instructions; Merge Assessment); `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/08-runtime-developer-findings.md` (Top Finding 1; Missing Information); `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/09-owner-findings.md` (Top Findings 2, 3; Scope Concerns).

`[S][LIB][ADR]` — **General agents entry-point variable** — Evaluate adding a project-agnostic `$GENERAL_AGENTS` variable to `$A_SOCIETY_INDEX` to simplify generic instruction-text references to a project's agents entry-point. Requires Owner direction on what the variable should point to (a new ready-made general agents.md template, an existing path, or a different approach) and whether a corresponding file in `general/` is warranted. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$A_SOCIETY_INDEX` in this design area; no merge. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 3).

`[S][ADR]` — **Runtime architecture design registration** — `$A_SOCIETY_RUNTIME_DEVELOPER_ROLE` requires reading "The approved Runtime Architecture Design document," which currently lives only in a record artifact (`a-society/a-docs/records/20260327-runtime-orchestrator-mvp/03-ta-to-owner.md`) with no index entry. Owner direction required: index at the current record path (unusual for a record artifact) or promote to a standing location (consistent with the `$A_SOCIETY_TOOLING_PROPOSAL` precedent). Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets the runtime architecture design document or its registration; no merge. Source: `a-society/a-docs/records/20260331-session-routing-removal/08-runtime-developer-findings.md` (Top Finding 1).


---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
