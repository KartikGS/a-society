# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` | Small — single-session maintenance change (one to two files) |
| `[M]` | Medium — multi-step change within a single phase |
| `[L]` | Large — multi-phase or multi-file strategic change |
| `[LIB]` | Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` | Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` | Direction decision involved; requires Owner-level scope |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `## Archive`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v12.0. General library Phase 0 gate closed: `$GENERAL_OWNER_ROLE` updated (workflow routing bullet names plan artifact as required gate; post-confirmation protocol replaced with three-point Phase 0 gate model); `$GENERAL_OWNER_WORKFLOW_PLAN_TEMPLATE` created, registered, and added to manifest (`required: true`); `$INSTRUCTION_WORKFLOW_COMPLEXITY` template pointer added. Framework update report published at v12.0 (2 Breaking, 1 Recommended). `$A_SOCIETY_RECORDS` artifact sequence corrected: `01-` is now `owner-workflow-plan.md`; brief shifts to `02-`; all subsequent positions shifted; Creating a Record Folder section updated. Approval Invariant violation flagged in backward pass: implementation preceded Phase 2 APPROVED; Items B/C/D (Brief-Writing Quality gate language + Approval Invariant enforcement gap) surfaced for Owner routing as next priorities. Compulsory complexity gate implemented: Phase 0 node added to A-Society workflow graph; `$A_SOCIETY_COMM_TEMPLATE_PLAN` created; `$A_SOCIETY_WORKFLOW`, `$A_SOCIETY_OWNER_ROLE`, `$A_SOCIETY_AGENT_DOCS_GUIDE` updated. No update report — all changes a-docs-internal. Initializer step 10 stale variable reference fixed. Fully agentic role model implemented (Breaking, v9.0). Improvement folder redesigned (Breaking, v8.0). Brief-Writing Quality section added to general Owner role template (v7.0). Nine targeted improvements to `$INSTRUCTION_WORKFLOW` (Breaking, v6.0). Handoff Output item 4 added to all role templates (Breaking, v5.0). Variable retirement protocol added (Recommended, v4.1). thinking/ folder required at initialization (Breaking, v4.0). Classification pre-specification prohibition added. Streamlined backward-pass entry added to `$A_SOCIETY_WORKFLOW`. Project log write authority formalized.

**Recent Focus**

`[M][LIB][MAINT]` — **General library Phase 0 gate and workflow plan template** (2026-03-17): Two general library gaps closed. *(1) `$GENERAL_OWNER_ROLE`* — Workflow routing bullet updated to name the plan artifact as required gate before any brief; post-confirmation protocol replaced with three-point Phase 0 gate model (plan before brief; tier-based routing — Tier 2/3 briefs downstream, Tier 1 implements directly). *(2) `$GENERAL_OWNER_WORKFLOW_PLAN_TEMPLATE`* — New file created at `general/communication/conversation/TEMPLATE-owner-workflow-plan.md`; registered in both public and internal indexes under `$GENERAL_OWNER_WORKFLOW_PLAN_TEMPLATE`; manifest entry added (`required: true`, `scaffold: copy`); `$INSTRUCTION_WORKFLOW_COMPLEXITY` "Producing a Workflow Plan" sentence updated with template pointer. Framework update report published at v12.0 (2 Breaking, 1 Recommended). Backward pass: `$A_SOCIETY_RECORDS` artifact sequence corrected inline (Curator-authority MAINT — 01- now plan, 02- brief, all downstream positions shifted; Creating a Record Folder updated; identifier format trigger corrected). Approval Invariant violation flagged: Curator implemented before APPROVED existed. Items B/C/D (Brief-Writing Quality gate language + Approval Invariant enforcement gap + Curator hard-rule interaction) surfaced for Owner routing. Record: `$A_SOCIETY_RECORDS/20260317-general-phase0-gate/`.

**Previous**

`[L][MAINT]` — **Compulsory complexity gate** (2026-03-17): Phase 0 formalised in the A-Society workflow graph. New node (`owner-phase-0-plan`) and edge (`owner-phase-0-plan → owner-phase-1-briefing`, artifact: `owner-workflow-plan`) added to YAML frontmatter. New Phase 0 section added to prose; Trigger Sources paragraph updated; Phase 1 Input updated; Handoffs table first row replaced with two rows; session model table and Step 1 updated. New template `TEMPLATE-owner-workflow-plan.md` created with machine-readable YAML frontmatter (five axes: `low | moderate | elevated | high`; tier: 1–3; path list; known_unknowns list). `$A_SOCIETY_OWNER_ROLE` workflow routing bullet and post-confirmation protocol updated to reference `$A_SOCIETY_COMM_TEMPLATE_PLAN`. `$A_SOCIETY_COMM_TEMPLATE_PLAN` registered in `$A_SOCIETY_INDEX`. `$A_SOCIETY_AGENT_DOCS_GUIDE` conversation folder entry updated. No update report — all changes a-docs-internal. Three triggers surfaced: `$GENERAL_OWNER_ROLE` Phase 0 gate; `$INSTRUCTION_WORKFLOW_COMPLEXITY` template reference; `agents.md` Tooling Developer summary drift. TA track for tooling enforcement mechanism also pending. Record: `$A_SOCIETY_RECORDS/20260317-compulsory-complexity-gate/`.

`[S][MAINT]` — **Initializer step 10 stale variable reference** (2026-03-14): `$GENERAL_IMPROVEMENT_PROTOCOL` reference removed from Phase 3 step 10; `improvement/protocol.md` removed from the step 10 artifact list and from the Handoff Criteria required-documents list. The file was merged into `improvement/main.md` in the improvement folder redesign; the Initializer was not updated at that time. No update report — change is internal to `$A_SOCIETY_INITIALIZER_ROLE` and has no impact on adopting projects (no adopting project Curator would follow the Initializer's step 10).

`[L][LIB][MAINT]` — **Fully agentic role model — human-collaborative phase flag** (2026-03-14): Three changes implemented. *(1) Role document framing* — `$INSTRUCTION_ROLES` opening definition updated: "A role document is a behavioral contract for an agent" (previously "human or agent contributor"); failure modes (Overreach, Paralysis) updated to "an agent" throughout. *(2) Workflow phase definition* — `$INSTRUCTION_WORKFLOW` Section 1 updated: Owner field note added (phase owner is always an agent role); `Human-collaborative` field added to phase definition format; Human-Collaborative Phase Pattern named sub-section added (three agent obligations: surface context, elicit contribution, author artifact; value format; structural rule); Step 2 in "How to Write One" updated with field mention and structural rule reference; conceptual node definition in "The Workflow as a Graph" updated to match. *(3) A-Society workflow MAINT* — `$A_SOCIETY_WORKFLOW` Phase 1 Owner corrected (Human → Owner for direction-change path); `Human-collaborative: direction` field added to Phase 1. Breaking update report published at v9.0. Record: `$A_SOCIETY_RECORDS/20260314-human-collaborative-phases/`.

---

## Next Priorities

1. `[S][LIB][MAINT]` — **Brief-Writing Quality language + Approval Invariant enforcement** — Backward pass from `20260317-general-phase0-gate` surfaced three related items (B, C, D): "proposal round becomes a confirmation step" language minimizes the approval gate; the authorization boundary is absent from the Curator's point of action (not in `$A_SOCIETY_CURATOR_ROLE` hard rules); brief registration scope vs. Curator hard-rule interaction is undocumented. Curator submits combined proposal; Owner reviews. Touches `$A_SOCIETY_OWNER_ROLE`, `$GENERAL_OWNER_ROLE`, `$A_SOCIETY_COMM_TEMPLATE_BRIEF`, `$A_SOCIETY_CURATOR_ROLE`.

2. `[S][LIB]` — **`$INSTRUCTION_RECORDS` Phase 0 alignment check** — The general records instruction may be out of step now that Phase 0 is formalized in the general Owner role template. Curator checks and proposes changes if needed; Owner reviews if LIB changes are warranted.

3. `[S][MAINT]` — **Public index tooling entries: `.js` → `.ts` extension correction** — All six existing tooling component entries in `$A_SOCIETY_PUBLIC_INDEX` use `.js` extensions; actual files on disk are `.ts`. Surfaced during Component 7 Track 2 registration (`20260318-tooling-enforcement-mechanism`). Path Validator (Component 5) would fail on all six entries. Curator corrects all six rows in a single maintenance pass; no Owner proposal needed (factual correction, Curator authority).

4. `[S][LIB][MAINT]` — **Existing-session handoff format** — All four roles in `20260318-tooling-enforcement-mechanism` independently flagged that handoff output sections document the new-session copyable prompt format but say nothing about existing-session handoffs — the common case for intra-flow transitions. User corrected multiple roles mid-flow. Define the named existing-session format (next action + artifacts to read + expected response, no role-assignment prompt) in all A-Society role docs and `general/roles/` templates. Curator proposes; Owner reviews. Touches `$A_SOCIETY_CURATOR_ROLE`, `$A_SOCIETY_TOOLING_DEVELOPER_ROLE`, `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`, `$GENERAL_CURATOR_ROLE`, `$GENERAL_OWNER_ROLE`, `$INSTRUCTION_ROLES`.

5. `[M][LIB][MAINT]` — **Multi-role backward pass ordering + Component 4 mandate** — `$A_SOCIETY_IMPROVEMENT` describes only the two-role Owner/Curator case; no generalizable rule exists for flows with additional roles. Owner computed the order manually and got it wrong twice. Component 4 (`$A_SOCIETY_TOOLING_BACKWARD_PASS_ORDERER`) was built for this but is not mandated by the protocol. Update `$A_SOCIETY_IMPROVEMENT` with the generalizable multi-role rule and require Component 4 invocation when available. Update `$A_SOCIETY_TOOLING_ADDENDUM` Phase 7 to require Component 4 invocation. Update `$GENERAL_IMPROVEMENT` in parallel. Curator proposes; Owner reviews.

6. `[S][MAINT]` — **INVOCATION.md update obligation + `a-docs/` dependency TA guidance** — When a new component is added, updating `tooling/INVOCATION.md` is not covered by the coupling map taxonomy (Type B covers `general/` instructions only) and is absent from the addendum Phase 7 Curator registration checklist — causing a miss in this flow. Also: the TA has no documented guidance on how to represent and recommend handling of an `a-docs/` format dependency in a component design. Touches `$A_SOCIETY_TOOLING_COUPLING_MAP`, `$A_SOCIETY_TOOLING_ADDENDUM`, `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`. Curator proposes; Owner reviews.

7. `[S][MAINT]` — **Brief-authoring conventions** — Two gaps surfaced in this flow: (1) when a brief bundles Curator-authority items with proposal-required items, there is no convention for marking each item's authority level — causing contradictory instructions in `08-owner-to-curator.md`; (2) the brief gave the Curator complete next-role-session instructions, enabling a bypass of the Owner-as-waypoint. Add item authority marking and a "condition not action" note to `$A_SOCIETY_COMM_TEMPLATE_BRIEF`. Add a hard rule to `$A_SOCIETY_CURATOR_ROLE`: return to Owner when gate condition is met; Owner authorizes the next session. Add standing note for implementation status when phase placement is an open question. Curator proposes; Owner reviews.

8. `[S][MAINT]` — **Records: decision artifact naming rule for non-Curator proposals** — The non-standard slot naming convention has no rule distinguishing "Owner handing work back to a role" from "Owner recording a decision after a role's work is complete." `04-owner-to-ta.md` was wrongly named; user caught it. Rule needed in `$A_SOCIETY_RECORDS`: use `NN-owner-decision.md` when the proposing role has no subsequent action; `NN-owner-to-[role].md` only when the named role has a next action. Curator proposes; Owner reviews.

9. `[M][MAINT]` — **Post-Phase-6 gate conditions + Developer completion artifact** — Addendum advisory mode and gate language was written for the original 6-component launch; does not describe conditions for subsequent component additions. Also: the Developer has no obligation to file a completion artifact — completion was recorded only as Owner paraphrase in `08-owner-to-curator.md`. Add a "Post-Phase-6 Component Additions" section to `$A_SOCIETY_TOOLING_ADDENDUM`. Update Developer role doc Phase 0 gate clause. Add completion report obligation (`NN-developer-completion.md`) to `$A_SOCIETY_TOOLING_DEVELOPER_ROLE` handoff output. Curator proposes; Owner reviews.

10. `[L]` — **Shared utils.ts + backward pass trigger prompt tool** — (1) `extractFrontmatter()` is duplicated across two components; no `tooling/src/utils.ts` exists for genuinely generic dependency-free functions. (2) The backward pass trigger prompt is fully deterministic from record folder + role list; manually composing it produced inconsistencies this flow. Scope (1) as a `tooling/src/utils.ts` boundary definition (TA) + deduplication (Developer). Scope (2) as a Component 4 extension: Backward Pass Orderer generates ordered role list and copyable trigger prompt per role. Both require TA scoping before Developer implementation. Open after Priorities 4–9 are complete.

---

## Archive

- `[L][LIB][MAINT]` — **Improvement folder redesign** (2026-03-14): Six design decisions implemented. *(1) Traversal algorithm* — Precise five-step backward pass ordering replaces vague "walk from terminal to entry": deduplicate by first occurrence, reverse, Owner second-to-last, synthesis role last, parallel forks concurrent. *(2) Synthesis path* — Two-path lightweight loop replaces main-workflow re-entry: Curator-authority items implement directly to a-docs; Owner-judgment items submit for approval before implementation. *(3) File collapse* — `$GENERAL_IMPROVEMENT_PROTOCOL` retired; philosophy and protocol merged into single `improvement/main.md`; index variable removed. *(4) Mandatory* — improvement/ folder required at initialization alongside thinking/; all deferral language removed. *(5) Generalizable findings* — Guidance added to flag project-agnostic findings explicitly in findings artifacts as potential framework contributions. *(6) Workflow backward pass mandatory* — Section 6 of `$INSTRUCTION_WORKFLOW` changed from "(recommended)" to "(mandatory)"; traversal ordering deferred to `$INSTRUCTION_IMPROVEMENT`; Owner-as-entry backward-pass ordering paragraph updated; Step 7 updated. Concurrent MAINT: `$A_SOCIETY_IMPROVEMENT` + `$A_SOCIETY_IMPROVEMENT_PROTOCOL` merged; `$A_SOCIETY_AGENT_DOCS_GUIDE` improvement entries updated; index updated (two variables retired). Breaking update report published at v8.0. Priority 1 MAINT resolved inline: phrase not found in `$GENERAL_IMPROVEMENT`; no change needed. Record: `$A_SOCIETY_RECORDS/20260314-improvement-folder-redesign/`.
- `[S][MAINT]` — **Internal maintenance bundle — Priorities 4–7** (2026-03-13): Four targeted MAINT changes closed. *(1) Initializer Phase 2 question quality* — Confirmation framing replaces open-ended description requests; options required where answers are bounded; tooling question added as mandatory. *(2) Workflow drift (three gaps)* — At-flow-close session rule added to "When to start a new session"; copyable artifact path and session-start prompt framing applied to steps 1–6 and human's role checklist; Owner-as-terminal-node named as a structural property of the flow. *(3) Records — non-standard slot naming and submissions-resolved* — Explicit `NN-[role]-[descriptor].md` naming convention for non-standard slots; submissions-resolved check promoted from mid-paragraph prose to named prerequisite. *(4) Curator role cross-layer framing and brief count-verify* — Standing Checks revised to distinguish within-scope drift (apply now) from out-of-scope drift (flag for future flow); count-verify note added to `$A_SOCIETY_COMM_TEMPLATE_BRIEF`. No update report — all changes internal to `a-docs/` and `agents/` with no impact on adopting projects. Record: `$A_SOCIETY_RECORDS/20260313-maint-bundle/`.
- `[L][LIB]` — **Workflow instruction improvements** (2026-03-14): Nine targeted improvements to `$INSTRUCTION_WORKFLOW`. Session model reclassified from recommended to mandatory for two or more roles (Breaking → v6.0 update report). Owner as workflow entry and terminal node — entry routes work in; terminal receives completion, acknowledges, and directs backward pass; delegated-authority exception named and scoped. Session reuse rules split: within-flow (resume by default) and at-flow-close (start fresh for next flow). Transition behavior: copyable artifact path always; copyable session-start prompt when new session required. Backward pass added as recommended section 6 — roles, ordering, findings destination. Branching split into conditional branching and parallel fork/join as distinct named sub-patterns. "Merged with role documents" failure mode corrected from visibility framing to process-rule fragmentation. Implementation constraint applied: branching sub-pattern split required by Owner review. Record: `$A_SOCIETY_RECORDS/20260314-workflow-instruction-improvements/`.
- `[M][LIB][MAINT]` — **Handoff Output — copyable session inputs** (2026-03-12): Priority 1 closed. Item 4 (copyable session inputs) added to all Handoff Output sections: `$INSTRUCTION_ROLES` (Section 7 canonical definition and all six archetype templates), `$GENERAL_OWNER_ROLE`, `$GENERAL_CURATOR_ROLE`. Concurrent MAINT: same addition to `$A_SOCIETY_OWNER_ROLE` and `$A_SOCIETY_CURATOR_ROLE`. Two design refinements during proposal: scope expanded from new-session-only to two-case model (always a copyable artifact path; session-start prompt only when a new session is required); `@` notation removed for tool-agnosticism. Breaking update report published at v5.0. Record: `$A_SOCIETY_RECORDS/20260312-handoff-copyable-prompt/`.
- `[S][ADR]` — **Streamlined backward-pass entry path and project log write authority** (2026-03-12): Two priorities closed (2 and 3). Four-condition backward-pass streamlined entry gate added to `$A_SOCIETY_WORKFLOW` Phase 1 — Curator may initiate at `01-curator-to-owner.md` without a preceding Owner brief when findings are aligned and direction is pre-established in the prior flow's findings artifacts. Project log write authority formalized: Curator owns lifecycle sections (Current State, Recent Focus, Previous, Archive) in `$A_SOCIETY_CURATOR_ROLE`; Owner owns Next Priorities in `$A_SOCIETY_OWNER_ROLE`. No update report — all changes internal to `a-docs/`. Record: `$A_SOCIETY_RECORDS/20260312-workflow-entry-and-log-ownership/`.
- `[S]` — **Initializer output hardening** (2026-03-12): Three priorities closed (2, 3, and 4). Phase 4 self-review extended with two absolute-path checks (role table paths and Phase 5 agents.md pointer). Phase 5 step 1 duplicate completion statement removed; step 2 relative path reminder added. Adversity log filing instruction added inside the `Consented: Yes` block. `$ONBOARDING_SIGNAL_TEMPLATE` Patterns Observed section hardened with contradiction guard. No update report — all changes internal to `$A_SOCIETY_INITIALIZER_ROLE` and `$ONBOARDING_SIGNAL_TEMPLATE`. Record: `$A_SOCIETY_RECORDS/20260312-initializer-output-hardening/`.
- `[M][LIB]` — **Variable retirement protocol + retirement scope in briefings** (2026-03-11): Two priorities closed (1 and 2). Variable Retirement section added to `$INSTRUCTION_INDEX`: five-step sequence (consumer grep → reference cleanup → guide-doc check → index-row removal → post-removal prose scan). Invariant 4 in `$A_SOCIETY_WORKFLOW` updated to name the retirement protocol as Invariant 4's counterpart. Recommended update report published at v4.1. Priority 2 (retirement scope in briefings) resolved cleanly as Step 3 of the sequence — no separate flow needed. Record: `$A_SOCIETY_RECORDS/20260311-variable-retirement-protocol/`.
- `[S][MAINT]` — **Curator maintenance bundle — template headers and cross-layer check** (2026-03-11): Two Curator-authority items closed. *(1) Template header scope* — Added "omit this header when instantiating" instruction to all three conversation templates. The brief template fix reads "omit all header blocks above the first `---`." No update report — a-docs/-only change. *(2) Self-consistency Curator practice* — Added "Standing Checks" section to `$A_SOCIETY_CURATOR_ROLE`: cross-layer consistency check requiring the Curator to verify `general/instructions/` and corresponding `a-docs/` artifacts align whenever working in either layer. No update report — a-docs/-only change. Record: `$A_SOCIETY_RECORDS/20260311-curator-maint-template-and-crosslayer/`.
- `[M][LIB][MAINT]` — **thinking/ required + classification prohibition** (2026-03-11): Two flows. *(1) thinking/ folder required at initialization* `[M][LIB]` — `$INSTRUCTION_THINKING` deferral path removed; thinking/ inserted as step 5 in Initializer Phase 3; Handoff Criteria updated; thinking/ rationale entry added to `$A_SOCIETY_AGENT_DOCS_GUIDE`. Breaking update report published at v4.0. Record: `$A_SOCIETY_RECORDS/20260311-thinking-folder-required/`. *(2) Classification pre-specification prohibition* `[S][MAINT]` — Owner role Brief-Writing Quality section extended: prohibition on pre-specifying update report classification in briefs or approval rationale. Reinforcing note added to `$A_SOCIETY_COMM_TEMPLATE_BRIEF`. No update report — a-docs/-only change. Record: `$A_SOCIETY_RECORDS/20260311-classification-prespec-prohibition/`.
- `[M][LIB][MAINT]` — **Initializer hardening — Phase 5 consent + quality gaps** (2026-03-11): Two flows from a test run of the Initializer against a seed-file project. (1) Phase 5 consent protocol hardening `[MAINT]` — Closed the consent bypass vulnerability: sequencing header added at the top of the Feedback Consent block; per-block wait pre-conditions added to all three consent conversations; report filing gate added to the onboarding signal block; "Consent verified" in Handoff Criteria redefined to require in-session human response (not file existence). No update report — Initializer behavior fix does not create gaps in existing a-docs. Record: `$A_SOCIETY_RECORDS/20260311-initializer-phase5-consent/`. (2) Initializer quality hardening `[M][LIB]` — Four gaps fixed across `$A_SOCIETY_INITIALIZER_ROLE` and `$INSTRUCTION_AGENTS`. Hard Rules: shell and version control prohibition added. Phase 2: scope boundary clarified. Phase 4: design-decision disclosure check added. `$INSTRUCTION_AGENTS`: reading order corrected; authority hierarchy specified. Framework update report: Breaking + Recommended → v3.0. Record: `$A_SOCIETY_RECORDS/20260311-initializer-quality-hardening/`.
- `[L][LIB][MAINT]` — **Feedback consent infrastructure + session improvements** (2026-03-11): Four flows across one session. (1) Feedback consent infrastructure wired end-to-end at v2.0; curator-signal template added; public index consolidated; Initializer Phase 5 restructured; update reports at v2.0 and v2.1. (2) Protocol and template improvements: five backward-pass learnings codified; Breaking classification extended; brief template hardened. (3) Decision template and Owner role: Follow-Up Actions section and Brief-Writing Quality section added. Records: `$A_SOCIETY_RECORDS/20260311-feedback-consent-infrastructure/`, `$A_SOCIETY_RECORDS/20260311-initializer-consent-registration/`, `$A_SOCIETY_RECORDS/20260311-protocol-and-template-improvements/`, `$A_SOCIETY_RECORDS/20260311-decision-template-improvements/`.
- `[S][MAINT]` — **Retire `todo/` folder — superseded by project log** (2026-03-10): Removed `a-docs/todo/` and its two completed artifacts, deleted both retired index variables from `$A_SOCIETY_INDEX`, removed the retired `todo/` rationale section from `$A_SOCIETY_AGENT_DOCS_GUIDE`, and cleared the remaining stale prose reference to `todo files` from the log rationale entry in the same guide. Backward pass reinforced Priority 1 (Variable retirement protocol): retirements need an active-docs scan for both `$VARIABLE` and prose references to the retired concept. Record: `$A_SOCIETY_RECORDS/20260310-retire-todo-folder/`.
- `[M][MAINT]` — **Briefing pre-approval language and Approval Invariant timing** (2026-03-10): Closed the gap that allowed a briefing to be read as implementation authorization. Four targeted changes: extended Approval Invariant 2 in `$A_SOCIETY_WORKFLOW` with timing and artifact-form requirement; added Phase 1 Input clarification that a briefing establishes scope/direction only; added authorization-scope note block to `$A_SOCIETY_COMM_TEMPLATE_BRIEF`; added implementation-authorization limit to the Owner → Curator section of `$A_SOCIETY_COMM_HANDOFF_PROTOCOL`. Record: `$A_SOCIETY_RECORDS/20260310-briefing-preapproval/`.
- `[S][MAINT]` — **Curator maintenance bundle** (2026-03-10): Closed three Curator-authority items. (1) Priority 6 — fixes already applied in a prior session; log entry closed. (2) Flow B — added Submission Requirements section to `$A_SOCIETY_UPDATES_PROTOCOL`; added Implementation Status section to `$A_SOCIETY_COMM_TEMPLATE_CURATOR_TO_OWNER`; updated mandatory field list in `$A_SOCIETY_COMM_HANDOFF_PROTOCOL`. (3) Flow C — added explicit within-flow sequencing rule to `$A_SOCIETY_RECORDS` and `$A_SOCIETY_COMM_HANDOFF_PROTOCOL`.
- `[L][LIB]` — **Handoff protocol and workflow-default routing** (2026-03-08): Made workflow routing the default Owner entry path, added mandatory `Handoff Output` guidance to the role-document standard, updated the Owner and Curator role implementations, clarified A-Society's session model, and published the Breaking framework update report `2026-03-08-handoff-protocol-routing.md`. Record: `$A_SOCIETY_RECORDS/20260308-handoff-protocol/`.
- `[S]` — **A-docs-guide rename** (2026-03-08): Renamed `agent-docs-guide.md` → `a-docs-guide.md` to align with A-Society's established naming convention. Updated all references and index variable. Record: `$A_SOCIETY_RECORDS/20260308-a-docs-guide-rename/`.
- `[M]` — **Records infrastructure** (2026-03-08): Established flow-level artifact tracking under `a-docs/records/`. Defined identifier format (`YYYYMMDD-slug`), artifact sequence, and placement rules. Retired `improvement/reports/` folder and four associated index variables. Record: `$A_SOCIETY_RECORDS/20260308-records-infrastructure/`.
- `[M][LIB]` — **Owner as universal session entry point** (2026-03-08): Added workflow routing responsibility and post-confirmation protocol to the Owner role in `$GENERAL_OWNER_ROLE` and `$A_SOCIETY_OWNER_ROLE`. Establishes the Owner as the default entry point for all project sessions. Framework update report: Recommended.
- `[L][LIB]` — **Graph-based workflow model** (2026-03-07): Extended the framework's single-thread workflow assumption to a full graph model supporting nodes, edges, instances, branching, and multi-graph projects. Six files updated in `general/instructions/`. Backwards compatible — single-thread projects unchanged. Framework update report: Recommended.
