# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` — Small — single-session maintenance change (one to two files) |
| `[M]` — Medium — multi-step change within a single phase |
| `[L]` — Large — multi-phase or multi-file strategic change |
| `[LIB]` — Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` — Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` — Direction decision involved; requires Owner-level scope |
| `[RUNTIME]` — Runtime layer change; follows Runtime Dev workflow |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v31.0.

**Recent Focus**

`[M][LIB][RUNTIME]` — **inflow-human-interaction** (2026-04-05): Runtime Dev flow closed. Added `type: prompt-human` typed handoff signal enabling autonomous in-flow agents to pause and request human input without flow suspension. `runtime/src/types.ts` — `{ kind: 'awaiting_human' }` added to `HandoffResult` union. `runtime/src/handoff.ts` — `prompt-human` case added to type-dispatch block; returns `{ kind: 'awaiting_human' }`. `runtime/src/orchestrator.ts` — `readHumanInput` private helper added (re-prompts on empty; resolves null on `exit`/`quit`/stream close); `awaiting_human` handling added to `advanceFlow` loop: on reply appends to `injectedHistory` and continues; on null sets `flowRun.status = 'awaiting_human'` and breaks. Also fixed: bootstrap Owner session changed from `autonomous: true` to `autonomous: false` — prevented bootstrap loop where Owner session was respawned without waiting for user input. `runtime/INVOCATION.md` — "In-Flow Human Interaction" section added; stream-close and empty-input re-prompt behaviors documented. `$INSTRUCTION_MACHINE_READABLE_HANDOFF` — new "Typed signal forms" section documents `prompt-human`, `forward-pass-closed`, and `meta-analysis-complete`; "When to Emit It" bullet corrected to direct agents to use `prompt-human` for clarification exchanges requiring a formal pause. Framework update report published (`a-society/updates/2026-04-05-in-flow-human-interaction.md`); version bumped v30.0 → v31.0 (Breaking). Closes: "Handoff-on-clarification restriction" Next Priority. Record: `a-society/a-docs/records/20260405-inflow-human-interaction/`.

**Previous**

`[M][RUNTIME]` — **runtime-session-ux** (2026-04-05): Runtime Dev flow closed. Three UX capabilities added to the runtime's interactive session layer. `runtime/src/types.ts` — `TurnUsage`, `TurnOptions`, `GatewayTurnResult` types added; `ProviderTurnResult` updated with `usage?` on both variants; `LLMProvider.executeTurn` updated with `options?: TurnOptions`; `LLMGatewayError` extended with `'ABORTED'` type and `partialText?` field. `runtime/src/spinner.ts` — new file; TTY-gated stderr spinner, no-op when non-TTY. `runtime/src/providers/anthropic.ts`, `runtime/src/providers/openai-compatible.ts` — abort signal threading via SDK RequestOptions; spinner integration; usage extraction per provider streaming protocol. `runtime/src/llm.ts` — return type changed from `Promise<string>` to `Promise<GatewayTurnResult>`; abort check at start of each tool-call round; usage accumulation across rounds; tool notifications moved to `process.stderr`. `runtime/src/orient.ts` — per-turn `AbortController` wired to readline SIGINT; `externalSignal?: AbortSignal` parameter for autonomous mode; `ABORTED` catch at all three call sites: `partialText` pushed to `providedHistory` directly on autonomous abort, to `history` on interactive abort; `[Aborted]` printed to stderr; session resumes in interactive mode, returns `null` in autonomous mode. `runtime/src/orchestrator.ts` — per-session `AbortController` with `process.once('SIGINT')` handler; `finally` cleanup removes listener unconditionally. `runtime/bin/a-society.ts` — `a-society flow-status` command added using `renderFlowStatus` / `parseWorkflow`. `runtime/INVOCATION.md` — updated operator documentation: interrupt, token display, `a-society flow-status`; `npm run a-society` removed. Curator: `$A_SOCIETY_RUNTIME_INVOCATION` description updated in both indexes to "entry points, CLI commands, and UX behaviors". Absorbed: "Runtime invocation / CLI surface convergence" Next Priority. Record: `a-society/a-docs/records/20260405-runtime-session-ux/`.

`[M][LIB][ADR][RUNTIME]` — **required-readings-authority** (2026-04-04): Multi-domain flow (Runtime Developer + Curator parallel tracks) closed. Closes the "Role-file required-reading authority model" Next Priority. *(A) Runtime Developer:* `runtime/src/registry.ts` — `buildRoleContext` refactored to read from `a-docs/roles/required-readings.yaml`; role identifier resolution updated to lowercase YAML keys. `runtime/src/injection.ts` — `buildContextBundle` updated to inject role announcement ("You are the [Role] agent for [project]") and current date before document injection. `runtime/src/orient.ts` — `autonomous` flag added to `runInteractiveSession` for automated error handling. `runtime/src/orchestrator.ts` — `advanceFlow` retry loop catches `HandoffParseError` and `WorkflowError`; errors appended to session history as user-turn messages for model self-correction. `runtime/INVOCATION.md` — "Configuration and Errors" section added. *(B) Curator:* Created `$INSTRUCTION_REQUIRED_READINGS` (`a-society/general/instructions/roles/required-readings.md`) — schema and maintenance protocol for required-readings.yaml. Updated `$GENERAL_OWNER_ROLE`, `$GENERAL_CURATOR_ROLE`, `$GENERAL_TA_ROLE` — removed `required_reading` frontmatter, `## Context Loading` sections, and context confirmation rituals. Updated `$INSTRUCTION_AGENTS` and `$INSTRUCTION_ROLES` — removed guidance directing authors to add required-reading lists or context confirmation statements; redirected to `required-readings.yaml` authority. Updated `$GENERAL_MANIFEST` — `roles/required-readings.yaml` added as required stub entry. Created `a-society/a-docs/roles/required-readings.yaml` (`$A_SOCIETY_REQUIRED_READINGS`) — A-Society's own instance. Removed frontmatter and ritual sections from all A-Society role files and `agents.md`; added Runtime Developer to agents.md roles table. Updated `$A_SOCIETY_INITIALIZER_ROLE` — Phase 3 updated to include production of `required-readings.yaml`. Updated `$A_SOCIETY_TOOLING_COUPLING_MAP` — Type A row added for `required-readings.yaml` runtime dependency. Registered `$A_SOCIETY_REQUIRED_READINGS` and `$INSTRUCTION_REQUIRED_READINGS` in both indexes. Updated `$A_SOCIETY_AGENT_DOCS_GUIDE`. Framework update report published (`a-society/updates/2026-04-04-required-readings-authority.md`); version bumped v29.0 → v30.0 (Breaking). Record: `a-society/a-docs/records/20260404-required-readings-authority/`.

`[S][RUNTIME]` — **runtime-state-isolation** (2026-04-04): Runtime Dev flow closed. `runtime/src/store.ts` — `getStateDir()` introduced; state directory configurable via `A_SOCIETY_STATE_DIR` env var (internal-use; operator documentation deferred pending identified use case). `runtime/src/orchestrator.ts` — project identity guard added in `startUnifiedOrchestration()` after `loadFlowRun()`: mismatched `projectRoot` emits warning and resets to bootstrap path rather than resuming stale flow. `runtime/test/integration/unified-routing.test.ts` — test state isolated to temp dir via env var; `stateVersion: '2'` added to flow fixture; temp dir cleaned up in `finally`. Record: `a-society/a-docs/records/20260404-runtime-state-isolation/`.


---

## Next Priorities

`[S][LIB]` — **Technical Architect advisory completeness addendum** — Add six generalizable TA advisory standards to `$GENERAL_TA_ROLE`: *(1)* require directory-scoped verification before asserting that a file exists or does not exist; *(2)* require advisories to name any newly required imports explicitly when specified behavior depends on them; *(3)* when an advisory reduces a node graph to a role order, require explicit per-role pinning semantics plus a worked trace covering repeated-role cases; *(4)* when an advisory claims an existing parser/regex already supports a format, require verification against the authoritative format instruction and worked example, not only the current implementation behavior; *(5)* require load-bearing identifier-mapping rules to be stated explicitly with worked examples and failure behavior; *(6)* require exact repo-relative paths in advisory file references and Files Changed tables. All six were implemented directly in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`; the reusable counterpart remains open for `$GENERAL_TA_ROLE`. Additionally: *(7)* add a Developer completion-report path portability obligation — completion reports and backward-pass findings must use repo-relative paths throughout; absolute paths and `file://` URLs are prohibited. This was implemented directly in `$A_SOCIETY_RUNTIME_DEVELOPER_ROLE` and `$A_SOCIETY_TOOLING_DEVELOPER_ROLE`; the reusable counterpart for `$GENERAL_TA_ROLE` (or a future general Developer role template) remains open. *(8)* add a brief-constraint evaluation obligation — when a brief contains design constraints, classify each as a hard constraint (derived from external dependencies, invariants, or explicit user direction) or a design preference (the Owner's current thinking) before anchoring to it; surface design preferences explicitly and present the alternative comparison before proceeding. Implemented directly in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`; the reusable counterpart remains open for `$GENERAL_TA_ROLE`. Follows Framework Dev workflow. Merge assessment: existing "Role guidance precision bundle" targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`; compatible authority level and workflow type, but different target file and TA-advisory completeness design area, so no merge. Updated by synthesis from `20260329-workflow-schema-unification-multi-domain`, `20260402-parallel-track-orchestration`, and `20260403-interactive-owner-session-handoff-routing` because the new findings target the same file (`$GENERAL_TA_ROLE`), the same TA-advisory completeness design area, compatible `[LIB]` authority, and the same Framework Dev workflow path. Updated by synthesis from `20260404-required-readings-authority` (path portability as Developer production obligation — same target file, compatible authority, same Framework Dev workflow path). Updated by synthesis from `20260405-inflow-human-interaction` (brief constraint evaluation — same target file `$GENERAL_TA_ROLE`, same TA-advisory completeness design area, compatible `[S][LIB]` authority, same Framework Dev workflow path). Source: `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 1); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/10-runtime-developer-findings.md` (Top Findings 3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/11-ta-findings.md` (Top Findings 2-3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/12-owner-findings.md` (Top Finding 4); `a-society/a-docs/records/20260402-parallel-track-orchestration/13a-tooling-developer-findings.md` (Finding 3; Generalizable Contribution); `a-society/a-docs/records/20260402-parallel-track-orchestration/13b-runtime-developer-findings.md` (Finding 1); `a-society/a-docs/records/20260402-parallel-track-orchestration/14-technical-architect-findings.md` (Top Findings 1-3); `a-society/a-docs/records/20260402-parallel-track-orchestration/15-owner-findings.md` (Top Finding 3; Workflow Friction); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/11-runtime-developer-findings.md` (Top Finding 1); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/12-ta-findings.md` (Top Finding 1; Workflow Friction); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/10-curator-findings.md` (Top Finding 3); `a-society/a-docs/records/20260404-required-readings-authority/13b-runtime-developer-findings.md` (Top Finding 1); `a-society/a-docs/records/20260404-required-readings-authority/14-owner-findings.md` (Top Finding 3; Workflow Friction); `a-society/a-docs/records/20260405-inflow-human-interaction/13-ta-findings.md` (Finding 3; Top Finding 1); `a-society/a-docs/records/20260405-inflow-human-interaction/14-owner-findings.md` (Top Finding 1; Generalizable Contributions).



`[S]` — **Tooling version-comparator hermeticity** — Decouple `tooling/test/version-comparator.test.ts` and its fixture set from the live framework `VERSION.md` so unrelated framework version bumps do not break tooling validation. Scope: choose a deterministic test boundary (test-local version fixture or equivalent pre-test alignment), update the affected tests/fixtures, and remove the need for developers to repair stale version fixtures during otherwise unrelated implementation flows. Follows Tooling Dev workflow. Merge assessment: no existing Next Priorities item targets `tooling/test/version-comparator.test.ts`, the version-comparator fixtures, or tooling-test hermeticity; no merge. Source: `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/08-tooling-developer-findings.md` (Top Finding 1; Missing Information; Top Finding 3; Workflow Friction).

`[M]` — **Machine-readable handoff validator (Component 8)** — New tooling component: extracts the `handoff` fenced block from agent pause-point output, parses the YAML, and validates field types against the current schema. Must support all three forms: single-object (`role` string, `artifact_path` string or null), the array form for fork points (list of `{role, artifact_path}` entries), and the typed signal forms (`type: forward-pass-closed`, `type: meta-analysis-complete`, `type: prompt-human`) added by the `parallel-track-orchestration` and `inflow-human-interaction` flows. Structurally analogous to Component 7 (Plan Artifact Validator) — same deterministic, rule-derived pattern, different input source. Follows standard Tooling Dev flow. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/03-curator-to-owner.md` (Tooling Validator Assessment).


`[M][RUNTIME]` — **Runtime integration test infrastructure** — The runtime layer still lacks a reusable harness and format standard for integration validation. Scope: create structured test utilities for conversational flows, including SSE stream mock helpers and lightweight project-fixture builders for registry/index traversal, and define a reproducible integration-record format that exercises real guard paths rather than ad hoc terminal transcripts. The coverage should include improvement-phase orchestration paths, not only minimal type or routing smoke tests. Follows Runtime Dev workflow. Merge assessment: updated by synthesis from `20260403-interactive-owner-session-handoff-routing`, `20260404-project-scoped-improvement-instructions`, and `20260404-runtime-state-isolation` because the new SSE-mocking, fixture-verbosity, improvement-phase coverage, and env-var cleanup findings target the same runtime integration-testing design area, compatible authority level, and the same Runtime Dev workflow path. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/11-runtime-developer-findings.md` (Top Findings 2-3; Missing Information; Workflow Friction); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/12-ta-findings.md` (Top Finding 2; Missing Information); `a-society/a-docs/records/20260404-project-scoped-improvement-instructions/07b-runtime-developer-finding.md` (Top Finding 2; Workflow Friction); `a-society/a-docs/records/20260404-runtime-state-isolation/05-owner-integration-gate.md` (Observation; env-var cleanup).

`[S][LIB][ADR]` — **General agents entry-point variable** — Evaluate adding a project-agnostic `$GENERAL_AGENTS` variable to `$A_SOCIETY_INDEX` to simplify generic instruction-text references to a project's agents entry-point. Requires Owner direction on what the variable should point to (a new ready-made general agents.md template, an existing path, or a different approach) and whether a corresponding file in `general/` is warranted. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$A_SOCIETY_INDEX` in this design area; no merge. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 3).


`[S][ADR]` — **Runtime architecture design registration** — `$A_SOCIETY_RUNTIME_DEVELOPER_ROLE` requires reading "The approved Runtime Architecture Design document," which currently lives only in a record artifact (`a-society/a-docs/records/20260327-runtime-orchestrator-mvp/03-ta-to-owner.md`) with no index entry. Scope: decide whether to index that record path directly (unusual for a record artifact) or promote the design to a standing location (consistent with the `$A_SOCIETY_TOOLING_PROPOSAL` precedent), and surface the load-bearing role-key-to-index-variable mapping contract with worked examples in that standing design so implementers do not have to rediscover it from code. Follows Framework Dev workflow. Merge assessment: updated by synthesis from `20260403-interactive-owner-session-handoff-routing` because the new mapping-contract finding targets the same standing runtime design artifact and design-registration area; merged in place. Source: `a-society/a-docs/records/20260331-session-routing-removal/08-runtime-developer-findings.md` (Top Finding 1); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/11-runtime-developer-findings.md` (Top Finding 1; Missing Information); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/12-ta-findings.md` (Top Finding 1; Missing Information).

`[M][LIB][ADR]` — **Workflow graph `human-collaborative` field status** — Evaluate whether `workflow.nodes[].human-collaborative` should remain a meaningful schema element, become optional purely-informational metadata, or be retired from the workflow graph schema now that the runtime treats node execution as conversational by default. Scope: resolve the field's authoritative semantics across workflow docs, `$INSTRUCTION_WORKFLOW_GRAPH`, tooling validation/coupling references, and any runtime interpretation. Follows the multi-domain development pattern (Framework Dev with Tooling Dev follow-through as needed). Merge assessment: no existing Next Priorities item targets the `human-collaborative` field, workflow graph schema semantics, or this retired-vs-informational design area; no merge. Source: `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/12-ta-findings.md` (Redundant Information; Top Finding 2); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/13-owner-findings.md` (Unclear Instructions; Redundant Information; Top Finding 2).

`[S][LIB]` — **Role-guidance precision follow-up: proposal-state claims and closure-time artifact precision** — Add ten reusable role-template clarifications. `$GENERAL_CURATOR_ROLE`: *(1)* when a brief makes a specific claim about the current state of a source document, verify the cited document during proposal preparation and flag unverifiable claims explicitly; *(2)* when uncertain whether a procedural step applies, flag the uncertainty explicitly and ask rather than inventing a justification — the escalation-first principle must be salient as the default response to procedural uncertainty. `$GENERAL_OWNER_ROLE` Brief-Writing Quality: *(1)* when a closure instruction specifies an update-report publish path, use the update-report filename contract `[YYYY-MM-DD]-[brief-descriptor].md` within the project's updates directory rather than a date-only filename; *(2)* before selecting a forward-pass closure artifact number, read the record folder and choose the next sequence slot rather than counting filenames, treating `a`/`b` sub-labels as shared slots; *(3)* when rolling the project log, archive the oldest `Previous` entry displaced by the shift rather than the flow currently closing; *(4)* when two viable implementation approaches exist and the Owner has a preference but not a mandate, name the preferred option, state why it is preferred, and require documented rationale if the non-preferred option is chosen ("preferred option + rationale requirement" brief pattern); *(5)* when a brief scopes both a removal and an additive item in the same target file, explicitly state for each removed section whether it is (a) removed entirely with no replacement or (b) replaced by a specific named new section — "Remove X; add Y" is ambiguous about whether Y replaces X or coexists with a stripped-down X; *(6)* when a brief targets a component that has distinct execution paths (interactive/autonomous, sync/async, TTY/non-TTY, etc.), explicitly declare which paths the requirement applies to — or state that all paths are in scope; do not rely on interactive-framing examples to convey full scope implicitly; *(7)* when a verification obligation confirms a documentation removal or output-format requirement, name the specific content that must be absent or the fields the output must include — "runs without error" is not sufficient; a command can execute successfully while still producing non-conformant output or retaining prohibited content; *(8)* when a brief asks a Technical Architect to produce a design, constraints in the brief must be genuinely non-negotiable (derived from framework invariants, explicit user direction, or immovable prior decisions); a design preference or working hypothesis is not a constraint — present it as a named preference with rationale and require the TA to address it, do not convert it into a prohibitive constraint; the test: "Would I reject a TA advisory that explored this option?" If the answer is "I don't know," it is not a constraint. Implemented directly in `$A_SOCIETY_OWNER_ROLE` Brief-Writing Quality; the reusable counterpart remains open for `$GENERAL_OWNER_ROLE`. Follows Framework Dev workflow. Merge assessment: updated in place by synthesis from `20260404-project-scoped-improvement-instructions` (closure-time precision findings) and `20260404-runtime-state-isolation` (option-delegation brief pattern) because both flows' findings target the same `$GENERAL_OWNER_ROLE` Brief-Writing Quality design area, compatible `[S][LIB]` authority level, and the same Framework Dev workflow path. Updated by synthesis from `20260404-required-readings-authority` ("remove entirely vs. replace" brief precision rule and escalation-first salience — same target files `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`, compatible authority, same Framework Dev workflow path). Updated by synthesis from `20260405-runtime-session-ux` (multi-mode scope declaration directive and verification content precision — same target file `$GENERAL_OWNER_ROLE` Brief-Writing Quality, compatible `[S][LIB]` authority, same Framework Dev workflow path; TA finding 3 and Owner findings 1–2 corroborate). Updated by synthesis from `20260405-inflow-human-interaction` (TA-design-brief constraint/preference partition — same target file `$GENERAL_OWNER_ROLE` Brief-Writing Quality, compatible `[S][LIB]` authority, same Framework Dev workflow path). Source: `a-society/a-docs/records/20260403-role-guidance-precision-bundle/06-curator-findings.md` (Finding 1; Missing Information; Finding 2; Workflow Friction); `a-society/a-docs/records/20260404-project-scoped-improvement-instructions/08-owner-finding.md` (Workflow Friction; Top Findings 2-3); `a-society/a-docs/records/20260404-runtime-state-isolation/11-owner-findings.md` (Top Finding 2; Generalizable Contributions); `a-society/a-docs/records/20260404-required-readings-authority/14-owner-findings.md` (Unclear Instructions; Scope Concerns; Analysis Quality; Top Findings 2 and 4); `a-society/a-docs/records/20260405-runtime-session-ux/14-ta-findings.md` (Top Finding 3; Missing Information); `a-society/a-docs/records/20260405-runtime-session-ux/15-owner-findings.md` (Top Findings 1–2; Missing Information; Workflow Friction); `a-society/a-docs/records/20260405-inflow-human-interaction/14-owner-findings.md` (Top Finding 1; Missing Information; Generalizable Contributions).


`[S][LIB]` — **Repo-relative path format convention (no leading slash)** — Add a path-format rule to `$INSTRUCTION_INDEX` specifying that repo-relative paths must omit the leading slash (e.g., `a-society/a-docs/agents.md`, not `/a-society/a-docs/agents.md`). Consistent no-leading-slash format prevents silent path-resolution failures in programmatic consumers (Tooling and Runtime layers). Fix existing mixed-format inconsistency in `$A_SOCIETY_PUBLIC_INDEX` as part of the same flow. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$INSTRUCTION_INDEX` path format, leading-slash convention, or `$A_SOCIETY_PUBLIC_INDEX` path format; no merge. Source: `a-society/a-docs/records/20260405-runtime-session-ux/12-curator-findings.md` (Top Finding 2; Missing Information).

`[S]` — **Tooling invocation repo-root execution note** — Update `$A_SOCIETY_TOOLING_INVOCATION` to state that `npx tsx` tooling commands should be run from the repository root using repo-relative paths, preventing `ERR_MODULE_NOT_FOUND` caused by CWD/path mismatches. Follows Tooling Dev workflow. Merge assessment: no existing Next Priorities item targets tooling-command CWD semantics, `npx tsx` repo-root execution guidance, or this operator-facing invocation note; no merge. Source: `a-society/a-docs/records/20260403-programmatic-improvement-system/13-tooling-finding.md` (Top Finding 1; Missing Information).

`[S][LIB]` — **Role identifier convention in `$INSTRUCTION_REQUIRED_READINGS`** — Add a "Role Key Convention" section to `$INSTRUCTION_REQUIRED_READINGS` specifying: keys must be lowercase hyphenated strings (e.g., `owner`, `technical-architect`, `runtime-developer`); keys must match the role name as used in the project's `agents.md` roles table, lowercased with spaces replaced by hyphens; include a worked example covering a compound name. Optionally document the runtime derivation rule (extract second segment of `namespace__Role Name`, lowercase, hyphenate) so implementers using the runtime's internal key format do not have to reverse-engineer the mapping. This gap was independently hit by both implementing roles in the flow that created the instruction. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$INSTRUCTION_REQUIRED_READINGS`, the role-key convention, or the identifier derivation design area; no merge. Source: `a-society/a-docs/records/20260404-required-readings-authority/13a-curator-findings.md` (Top Finding 2; Unclear Instructions); `a-society/a-docs/records/20260404-required-readings-authority/13b-runtime-developer-findings.md` (Top Finding 2; Missing Information); `a-society/a-docs/records/20260404-required-readings-authority/14-owner-findings.md` (Top Finding 1; Missing Information).

`[S][LIB]` — **Repeated-header matching guidance in `$GENERAL_IMPROVEMENT_META_ANALYSIS`** — Add guidance to `$GENERAL_IMPROVEMENT_META_ANALYSIS` (or an equivalent implementation instruction) that when editing files with repeated semantic sub-headers (e.g., `### Roles` appearing under multiple parent `## Section` headings), agents must include the parent section header in their match context to ensure placement integrity. A mis-edit during `a-docs-guide.md` modification — where a `### Roles` header was misplaced due to ambiguous match context — is the motivating case. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$GENERAL_IMPROVEMENT_META_ANALYSIS`, repeated-header edit discipline, or this design area; no merge. Source: `a-society/a-docs/records/20260404-required-readings-authority/13a-curator-findings.md` (Top Finding 1; Analysis Quality; Framework Contribution Candidate 2).


---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
