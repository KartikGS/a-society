# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` | Small — single-session maintenance change (one to two files) |
| `[M]` | Medium — multi-step change within a single phase |
| `[L]` | Large — multi-phase or multi-file strategic change |
| `[LIB]` | Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` | Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` | Direction decision involved; requires Owner-level scope |
| `[RUNTIME]` | Runtime layer change; follows Runtime Dev workflow |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v26.0.

**Recent Focus**

`[S][RUNTIME]` — **registry-frontmatter-reader** (2026-03-30): Runtime Dev flow closed. `runtime/src/registry.ts` — static `roleContextRegistry` replaced with `buildRoleContext(roleKey, projectRoot)` that reads `universal_required_reading` from `agents.md` and `required_reading` from the role file via YAML frontmatter parsing (`js-yaml`), using `resolveVariableFromIndex()`; `RoleContextEntry.namespace` field removed; `roleKeyToIndexVariable` private helper added (derives `$NAMESPACE_ROLENAME_ROLE` from roleKey); null returned for all hard failures, universal-only list returned for absent `required_reading`. `runtime/src/injection.ts` and `runtime/src/orient.ts` — callers updated to `buildRoleContext`. `runtime/src/paths.ts` — regex bug fixed (`[\$A_Z_0-9]` → `\$[\w-]+`), enabling resolution of variables with letters B–Y. No `general/` changes; no framework update report. Backward pass complete (Curator synthesis, 2026-03-30). Synthesis MAINT: `$A_SOCIETY_RECORDS` now requires the Owner intake node to appear first in `workflow.md`; `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` now requires verification of declared-operational dependency correctness and explicit operator-facing log ownership for shared error-propagation paths. New Next Priorities: role-file required-reading authority model. Record: `a-society/a-docs/records/20260330-registry-frontmatter-reader/`.

**Previous**

`[M][LIB]` — **owner-protocol-and-role-guidance-bundle** (2026-03-29): Framework Dev flow closed. Eleven items across three groups. *(Group A — Log Validity Sweep):* `$GENERAL_OWNER_ROLE` and `$A_SOCIETY_OWNER_ROLE` — Intake Validity Sweep added to Post-Confirmation Protocol (four-case taxonomy: addressed, contradicted, restructured, partially addressed; scope assessed by Owner, not human's stated intent); Closure Validity Sweep added to Forward Pass Closure Discipline section. `$INSTRUCTION_LOG` — Validity Sweeps subsection added to Entry Lifecycle. *(Group B — Merge Criteria):* Criterion 3 updated in `$GENERAL_OWNER_ROLE`, `$A_SOCIETY_OWNER_ROLE`, and `$INSTRUCTION_LOG` — now reads "same workflow type and role path, or routable as parallel tracks in a single multi-domain flow." *(Group C — Role Guidance Precision):* `$GENERAL_OWNER_ROLE` — prose-insertion immediately adjacent anchor requirement; instruction-text real variable names; mirror assessment for project-specific convention changes; schema-migration vocabulary sweep; Forward Pass Closure Discipline section created (log-before-closure + closure sweep); Constraint-Writing Quality section added (file-scoped registration). `$GENERAL_CURATOR_ROLE` — proposal-stage rendered-content matching; implementation-stage terminology sweep for schema changes. `$A_SOCIETY_CURATOR_ROLE` — parity confirmed, no changes required. Framework update report published (Breaking, v25.0 → v26.0). Backward pass initiated. Record: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/`.

`[M][LIB][ADR]` — **workflow-schema-unification-multi-domain** (2026-03-29): Single multi-domain flow (three parallel tracks) closed. *(A) Tooling Dev:* Component 4 (`backward-pass-orderer.ts`) updated to nodes/edges schema — `WorkflowNode`/`WorkflowEdge` interfaces, node-list-order first-occurrence algorithm, updated `computeBackwardPassOrder` signature, hard-fail on legacy `path[]` schema; `tooling/INVOCATION.md` Component 4 section rewritten; Component 7 (`validatePlanArtifact`) entry added to INVOCATION.md; coupling map "Tooling Dev pending" note resolved. *(B) Runtime Dev:* `workflowDocumentPath` removed from `FlowRun` type, `startFlow` signature, and CLI; `advanceFlow` and `flowStatus()` updated to derive path from `recordFolderPath/workflow.md`; `runtime/INVOCATION.md` `start-flow` signature updated to four args. *(C) Framework Dev:* `$INSTRUCTION_WORKFLOW` — new "Multi-domain parallel-track flows" subsection added under Extended Workflow Patterns; `$A_SOCIETY_WORKFLOW_MULTI_DOMAIN` created (`a-docs/workflow/multi-domain-development.md`), registered in `$A_SOCIETY_INDEX`; `$A_SOCIETY_WORKFLOW` updated with Multi-domain pattern section and Session Routing cross-reference. Framework update report published (v24.1 → v25.0, Breaking + Recommended): Breaking — `$GENERAL_OWNER_ROLE` "separate flows per workflow type" rule removed, replaced with single-flow parallel-track guidance; Recommended — `$INSTRUCTION_WORKFLOW` multi-domain subsection. Also: Component 6 `compareVersions` ascending sort fix (Curator boundary overrun — backward pass finding). Backward pass complete (Curator synthesis, 2026-03-29). Synthesis MAINT: `$A_SOCIETY_WORKFLOW_TOOLING_DEV` now limits Curator Phase 7 registration to documentation/registration artifacts and requires an explicit Owner exception or Tooling Developer handoff before any `tooling/src` or tooling-test edit; `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` now requires advisories to name newly required imports explicitly when specified behavior depends on them. Record: `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/`.

`[M][LIB]` — **workflow-schema-unification** (2026-03-29): Framework Dev flow closed (Phase 1 of 3 in multi-flow schema unification). Four items: *(1)* `$A_SOCIETY_RECORDS` — workflow.md schema updated to nodes/edges format; completeness obligation updated to node/edge terminology; "What Component 4 reads" and "Relationship to plan's path" passages updated; *(2)* `$INSTRUCTION_RECORDS` — same schema update; `synthesis_role` removed; cross-reference to `$INSTRUCTION_WORKFLOW_GRAPH` added for schema definition; completeness obligation, Step 2, and plan relationship passages updated; *(3)* `$INSTRUCTION_WORKFLOW_GRAPH` — new "Record-Folder workflow.md — Subgraph Variant" section appended; *(4)* `$A_SOCIETY_TOOLING_COUPLING_MAP` — Component 4 row updated to nodes/edges schema with Tooling Dev pending note. Framework update report published (Recommended, v24.0 → v24.1). Backward pass complete (Curator synthesis, 2026-03-29). Synthesis MAINT: `$A_SOCIETY_OWNER_ROLE` now requires brief-time mirror assessment when project-specific conventions instantiate general instructions and requires schema-migration briefs to scope a vocabulary sweep; `$A_SOCIETY_CURATOR_ROLE` now requires an implementation-stage terminology sweep for schema changes. Record: `a-society/a-docs/records/20260329-workflow-schema-unification/`.

---

## Next Priorities

`[M][LIB][ADR]` — **Role-file required-reading authority model** — Evaluate how role documents should serve both runtime parsing and human/manual orientation without maintaining two authoritative required-reading lists. Scope: decide whether YAML `required_reading` / `universal_required_reading` becomes the sole authority with prose deferring to it, or whether another structure better separates machine-readable and natural-language consumers; propagate the decision across A-Society role docs and the corresponding `general/` role templates as needed. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets role-file required-reading structure, YAML/prose authority, or the runtime/manual dual-consumer role-doc design area; no merge. Source: `a-society/a-docs/records/20260330-registry-frontmatter-reader/09-curator-findings.md` (Top Finding 1; Redundant Information); `a-society/a-docs/records/20260330-registry-frontmatter-reader/10-runtime-developer-findings.md` (Top Finding 2; Redundant Information); `a-society/a-docs/records/20260330-registry-frontmatter-reader/12-owner-findings.md` (Top Finding 3; Redundant Information).

`[S][LIB]` — **Technical Architect advisory completeness addendum** — Add two generalizable TA advisory standards to `$GENERAL_TA_ROLE`: *(1)* require directory-scoped verification before asserting that a file exists or does not exist; *(2)* require advisories to name any newly required imports explicitly when specified behavior depends on them. Both were implemented directly in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`; the reusable counterpart remains open for `$GENERAL_TA_ROLE`. Follows Framework Dev workflow. Merge assessment: existing "Role guidance precision bundle" targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`; compatible authority level and workflow type, but different target file and TA-advisory completeness design area, so no merge. Updated by synthesis from `20260329-workflow-schema-unification-multi-domain` because the new finding targets the same file (`$GENERAL_TA_ROLE`), the same TA-advisory completeness design area, compatible `[LIB]` authority, and the same Framework Dev workflow path. Source: `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 1); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/10-runtime-developer-findings.md` (Top Findings 3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/11-ta-findings.md` (Top Findings 2-3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/12-owner-findings.md` (Top Findings 4).

`[M]` — **Component 4 parallel-flow correctness bundle** — Update the Backward Pass Orderer so backward traversal is derived from the workflow graph rather than flat node-list first occurrence when a flow contains parallel forks, preserving concurrent backward-pass steps per `$GENERAL_IMPROVEMENT`. In the same flow, update Component 4 prompt generation so backward-pass prompts direct agents to inspect the record folder for the next available sequence number instead of predicting filenames. Follows Tooling Dev workflow. Merge assessment: no existing Next Priorities item targets `backward-pass-orderer.ts` or the Component 4 traversal/prompt-generation design area, so no merge. The two findings are bundled because they target the same component, require the same Tooling Dev workflow path, and both correct cases where Component 4 output diverges from the improvement protocol. Source: `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/09-owner-forward-pass-closure.md` (Component 4 invocation override note); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/11-curator-findings.md` (Top Findings 2, 4); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/12-owner-findings.md` (Top Findings 1, 3).

`[M]` — **Machine-readable handoff validator (Component 8)** — New tooling component: extracts the `handoff` fenced block from agent pause-point output, parses the YAML, and validates field types, enum constraints, and the `prompt` conditional against `session_action`. Structurally analogous to Component 7 (Plan Artifact Validator) — same deterministic, rule-derived pattern, different input source. Follows standard Tooling Dev flow. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/03-curator-to-owner.md` (Tooling Validator Assessment).


`[M]` — **Runtime integration test infrastructure** — The runtime layer has no test harness or format standard for integration validation. The integration gate requires a reproducible test record; the Developer improvised a manual terminal transcript that could not exercise the registry guard path (mock API key short-circuited before that path ran). A structured test harness would have surfaced the missing guard at validation time rather than in the backward pass. Follows Runtime Dev workflow. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2).

`[S][LIB]` — **Role guidance precision bundle: brief-state-claim verification and implementation portability gap** — Two role-file improvements surfaced by backward pass of `20260329-owner-protocol-and-role-guidance-bundle`: *(1)* Add a "verify current file state before making specific state claims" obligation to the Brief-Writing Quality section of `$GENERAL_OWNER_ROLE` and `$A_SOCIETY_OWNER_ROLE` — mirrors the existing Review Artifact Quality re-read rule; prevents the same class of error (asserting a specific passage or criterion exists without reading the target file) at brief-writing time. *(2)* Add an implementation-stage portability check to `$GENERAL_CURATOR_ROLE` and `$A_SOCIETY_CURATOR_ROLE` Implementation Practices — when adapting text from a project-specific source for a general template (or vice versa), explicitly verify that variable references, terminology, and examples are valid in the target context. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$GENERAL_OWNER_ROLE` (Brief-Writing Quality) or `$GENERAL_CURATOR_ROLE` (Implementation Practices). The TA advisory addendum targets `$GENERAL_TA_ROLE` — different file and design area; no merge. The two findings are bundled because they target the same general-layer role files, share `[LIB]` authority, and follow the same Framework Dev workflow path. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 1); `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/07-owner-findings.md` (Top Findings 1, 2).

`[S][LIB][ADR]` — **General agents entry-point variable** — Evaluate adding a project-agnostic `$GENERAL_AGENTS` variable to `$A_SOCIETY_INDEX` to simplify generic instruction-text references to a project's agents entry-point. Requires Owner direction on what the variable should point to (a new ready-made general agents.md template, an existing path, or a different approach) and whether a corresponding file in `general/` is warranted. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$A_SOCIETY_INDEX` in this design area; no merge. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 3).

`[M][LIB][ADR]` — **Workflow-derived synthesis role routing** — Revisit the runtime's `synthesisRole` source of truth. The current env-var approach works for A-Society's current flows but couples routing to deployment configuration; the long-term correct home is the workflow artifact layer rather than the host environment. Expected scope: Runtime Dev design reconsideration plus any required workflow-graph schema and instruction updates. Follows Runtime Dev workflow with TA advisory and Owner gate; may require Framework Dev follow-on if the workflow-graph schema changes. Merge assessment: existing runtime Next Priorities items target test infrastructure, invocation registration, or `a-docs/` dependency tracking; different files and design area, so no merge. Source: `a-society/a-docs/records/20260328-runtime-env-bundle/12-runtime-developer-findings.md` (Finding 2); `a-society/a-docs/records/20260328-runtime-env-bundle/13-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 3).

---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
