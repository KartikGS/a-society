# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` — Small — single-session maintenance change (one to two files) |
| `[M]` — Medium — multi-step change within a single phase |
| `[L]` — Large — multi-phase or multi-file strategic change |
| `[LIB]` — Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` — Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` — Direction decision involved; requires Owner-level scope |
| `[RUNTIME]` — Runtime layer change; follows Runtime Dev workflow |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v27.2.

**Recent Focus**

`[M][LIB]` — **role-guidance-precision-bundle** (2026-04-03): Framework Dev flow closed. *(A) Curator:* Added 7 precision rules to `$GENERAL_OWNER_ROLE` — removed element consumer enumeration, structured-entry replacement boundary, removal-of-dependents scoping, review artifact quality, schema-code coupling check, index variable retirement reference sweep, and multi-track path portability — plus 1 rule to `$GENERAL_CURATOR_ROLE` for proposal-stage implementation portability checks. Version bump v27.1 → v27.2. Framework update report filed (`a-society/updates/2026-04-03-role-guidance-precision-bundle.md`). *(B) Backward pass synthesis:* added the A-Society-side structured-entry replacement boundary, Curator proposal-stage source-claim verification, and Owner closure-time update-report path naming rule; queued a reusable general-role follow-up for proposal-state-claim verification and update-report filename compliance. Record: `a-society/a-docs/records/20260403-role-guidance-precision-bundle/`.

**Previous**

`[L][LIB][RUNTIME]` — **parallel-track-orchestration** (2026-04-02): Multi-domain flow (TA advisory → Tooling Dev + Runtime Dev parallel tracks → Curator documentation) closed. *(A) Tooling Developer:* `tooling/src/backward-pass-orderer.ts` — Component 4 algorithm replaced: BFS-from-terminals through predecessor links computes reverse distance; nodes at the same distance form concurrent groups; `computeBackwardPassOrder` now requires `edges` parameter; return type changed from `BackwardPassEntry[]` to `BackwardPassEntry[][]` (`BackwardPassPlan`); `createMetaAnalysisPrompt` gains `concurrent` flag emitting sub-labeled findings note for parallel steps; `orderWithPromptsFromFile` threads `frontmatter.workflow.edges`. `tooling/INVOCATION.md` updated with new algorithm description, signature, and output type. `tooling/test/backward-pass-orderer.test.ts` expanded with parallel graph and concurrent group tests. `tooling/test/integration.test.ts` updated for 2D plan type. *(B) Runtime Developer:* `runtime/src/types.ts` — `FlowRun.currentNode` replaced with `activeNodes: string[]`, `completedNodes: string[]`, `completedNodeArtifacts: Record<string, string>`, `pendingNodeArtifacts: Record<string, string[]>`. `runtime/src/handoff.ts` — `HandoffBlock` replaced with `HandoffTarget`; `parse()` returns `HandoffTarget[]`; supports both single-object and array forms. `runtime/src/orchestrator.ts` — `advanceFlow(flowRun, nodeId, activeArtifactPath?, humanInput?)` with fork detection (strict handoff-count match), join detection (`activateOrDefer` checking all predecessors), node-keyed sessions (`flowId__nodeId`), and terminal-with-active-tracks handling. `runtime/src/injection.ts` — `activeArtifactPath: string | string[]` with numbered headers. `runtime/src/visualization.ts` (new) — `renderFlowStatus` with active/completed/pending-join display. `runtime/src/cli.ts` — `resume-flow` takes `nodeId`, `flow-status` uses visualization. `runtime/INVOCATION.md` updated. Runtime tests added (handoff: 7, visualization: 3, types: 1). *(C) Curator:* `$INSTRUCTION_MACHINE_READABLE_HANDOFF` updated with fork-point array form schema, form selection rule, and worked examples. `$A_SOCIETY_TOOLING_COUPLING_MAP` — Component 4 Type C note (2026-04-02). Framework update report published (Recommended × 1, v27.0 → v27.1). Absorbs Component 4 parallel-flow correctness bundle from Next Priorities. Record: `a-society/a-docs/records/20260402-parallel-track-orchestration/`.

`[M][LIB][RUNTIME]` — **c7-removal-c3-extension-synthesis-hardcode** (2026-04-01): Multi-domain flow (Tooling Dev + Runtime Dev parallel tracks) closed. *(A) Tooling Developer:* `tooling/src/workflow-graph-validator.ts` — `validateWorkflowFile` extended with optional `strict?: boolean`; three structural invariant checks added: neighboring same-role nodes (unconditional), Owner-at-start and Owner-at-end (strict mode only); start/end nodes identified by incoming/outgoing edge absence. `tooling/src/plan-artifact-validator.ts` and `tooling/test/plan-artifact-validator.test.ts` deleted (Component 7 removed). Version-comparator test fixtures updated to v27.0 (pre-existing drift resolved). *(B) Runtime Developer:* `runtime/src/triggers.ts` — `ACTIVE_ARTIFACT` handler and `validatePlanArtifact` import removed; Component 3 invocation updated to `strict: true`; synthesis role hardcoded to `'Curator'`. `runtime/src/orchestrator.ts` — `ACTIVE_ARTIFACT` trigger call removed (type safety). `runtime/.env.sample` — `SYNTHESIS_ROLE` block and now-empty section header removed. *(C) Curator:* `tooling/INVOCATION.md`, `runtime/INVOCATION.md`, `$A_SOCIETY_ARCHITECTURE`, `$A_SOCIETY_TOOLING_COUPLING_MAP`, `$A_SOCIETY_TOOLING_PROPOSAL`, `$A_SOCIETY_WORKFLOW_TOOLING_DEV`, `$A_SOCIETY_PUBLIC_INDEX` updated for Component 7 removal and Component 3 extension. `$INSTRUCTION_WORKFLOW_COMPLEXITY` — dangling Component 7 reference removed (Curator-authority maintenance; no framework update report). Record: `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/`.

`[S][RUNTIME]` — **orient-startup-simplification** (2026-04-01): Runtime Dev flow closed. `runtime/src/orient.ts` — initial message updated to direct the Owner to read the project log and give a brief status before asking what the user wants to work on; startup console log removed; close message updated from "Orient session closed." to "Session closed." `runtime/INVOCATION.md` — `## Interactive Entry Point` section added documenting the `a-society` binary as the recommended entry point for interactive Owner sessions; `orient` command description updated to classify it as the underlying low-level mechanism. No `general/` changes; no framework update report. Record: `a-society/a-docs/records/20260401-orient-startup-simplification/`.

---

## Next Priorities

`[M][LIB][ADR]` — **Role-file required-reading authority model** — Evaluate how role documents should serve both runtime parsing and human/manual orientation without maintaining two authoritative required-reading lists. Scope: decide whether YAML `required_reading` / `universal_required_reading` becomes the sole authority with prose deferring to it, or whether another structure better separates machine-readable and natural-language consumers; propagate the decision across A-Society role docs and the corresponding `general/` role templates as needed. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets role-file required-reading structure, YAML/prose authority, or the runtime/manual dual-consumer role-doc design area; no merge. Source: `a-society/a-docs/records/20260330-registry-frontmatter-reader/09-curator-findings.md` (Top Finding 1; Redundant Information); `a-society/a-docs/records/20260330-registry-frontmatter-reader/10-runtime-developer-findings.md` (Top Finding 2; Redundant Information); `a-society/a-docs/records/20260330-registry-frontmatter-reader/12-owner-findings.md` (Top Finding 3; Redundant Information).

`[S][LIB]` — **Technical Architect advisory completeness addendum** — Add four generalizable TA advisory standards to `$GENERAL_TA_ROLE`: *(1)* require directory-scoped verification before asserting that a file exists or does not exist; *(2)* require advisories to name any newly required imports explicitly when specified behavior depends on them; *(3)* when an advisory reduces a node graph to a role order, require explicit per-role pinning semantics plus a worked trace covering repeated-role cases; *(4)* when an advisory claims an existing parser/regex already supports a format, require verification against the authoritative format instruction and worked example, not only the current implementation behavior. All four were implemented directly in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`; the reusable counterpart remains open for `$GENERAL_TA_ROLE`. Follows Framework Dev workflow. Merge assessment: existing "Role guidance precision bundle" targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`; compatible authority level and workflow type, but different target file and TA-advisory completeness design area, so no merge. Updated by synthesis from `20260329-workflow-schema-unification-multi-domain` and `20260402-parallel-track-orchestration` because the new findings target the same file (`$GENERAL_TA_ROLE`), the same TA-advisory completeness design area, compatible `[LIB]` authority, and the same Framework Dev workflow path. Source: `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 1); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/10-runtime-developer-findings.md` (Top Findings 3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/11-ta-findings.md` (Top Findings 2-3); `a-society/a-docs/records/20260329-workflow-schema-unification-multi-domain/12-owner-findings.md` (Top Finding 4); `a-society/a-docs/records/20260402-parallel-track-orchestration/13a-tooling-developer-findings.md` (Finding 3; Generalizable Contribution); `a-society/a-docs/records/20260402-parallel-track-orchestration/13b-runtime-developer-findings.md` (Finding 1); `a-society/a-docs/records/20260402-parallel-track-orchestration/14-technical-architect-findings.md` (Top Findings 1-3); `a-society/a-docs/records/20260402-parallel-track-orchestration/15-owner-findings.md` (Top Finding 3; Workflow Friction).



`[S]` — **Tooling version-comparator hermeticity** — Decouple `tooling/test/version-comparator.test.ts` and its fixture set from the live framework `VERSION.md` so unrelated framework version bumps do not break tooling validation. Scope: choose a deterministic test boundary (test-local version fixture or equivalent pre-test alignment), update the affected tests/fixtures, and remove the need for developers to repair stale version fixtures during otherwise unrelated implementation flows. Follows Tooling Dev workflow. Merge assessment: no existing Next Priorities item targets `tooling/test/version-comparator.test.ts`, the version-comparator fixtures, or tooling-test hermeticity; no merge. Source: `a-society/a-docs/records/20260401-c7-removal-c3-extension-synthesis-hardcode/08-tooling-developer-findings.md` (Top Finding 1; Missing Information; Top Finding 3; Workflow Friction).

`[M]` — **Machine-readable handoff validator (Component 8)** — New tooling component: extracts the `handoff` fenced block from agent pause-point output, parses the YAML, and validates field types against the current schema. Must support both forms: single-object (`role` string, `artifact_path` string or null) and the array form for fork points (list of `{role, artifact_path}` entries) added by the `parallel-track-orchestration` flow (2026-04-02). Structurally analogous to Component 7 (Plan Artifact Validator) — same deterministic, rule-derived pattern, different input source. Follows standard Tooling Dev flow. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/03-curator-to-owner.md` (Tooling Validator Assessment).


`[M]` — **Runtime integration test infrastructure** — The runtime layer has no test harness or format standard for integration validation. The integration gate requires a reproducible test record; the Developer improvised a manual terminal transcript that could not exercise the registry guard path (mock API key short-circuited before that path ran). A structured test harness would have surfaced the missing guard at validation time rather than in the backward pass. Follows Runtime Dev workflow. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2).

`[M][RUNTIME]` — **Runtime persisted-state versioning and migration checks** — Add an explicit persistence version boundary for runtime-managed flow state so resumed flows do not rely on silent structural compatibility. Scope: version the persisted `FlowRun` shape (or equivalent runtime state object), add a load-time compatibility check in the session store / resume path, and define the reset-or-migrate behavior when stored state predates the current structure. Follows Runtime Dev workflow. Merge assessment: no existing Next Priorities item targets runtime persisted-state shape, `FlowRun` compatibility, or session-store migration handling; no merge. Source: `a-society/a-docs/records/20260402-parallel-track-orchestration/13b-runtime-developer-findings.md` (Finding 2; Generalizable Contribution); `a-society/a-docs/records/20260402-parallel-track-orchestration/15-owner-findings.md` (Generalizable Findings).

`[S][LIB][ADR]` — **General agents entry-point variable** — Evaluate adding a project-agnostic `$GENERAL_AGENTS` variable to `$A_SOCIETY_INDEX` to simplify generic instruction-text references to a project's agents entry-point. Requires Owner direction on what the variable should point to (a new ready-made general agents.md template, an existing path, or a different approach) and whether a corresponding file in `general/` is warranted. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$A_SOCIETY_INDEX` in this design area; no merge. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 3).

`[S][ADR]` — **Runtime architecture design registration** — `$A_SOCIETY_RUNTIME_DEVELOPER_ROLE` requires reading "The approved Runtime Architecture Design document," which currently lives only in a record artifact (`a-society/a-docs/records/20260327-runtime-orchestrator-mvp/03-ta-to-owner.md`) with no index entry. Owner direction required: index at the current record path (unusual for a record artifact) or promote to a standing location (consistent with the `$A_SOCIETY_TOOLING_PROPOSAL` precedent). Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets the runtime architecture design document or its registration; no merge. Source: `a-society/a-docs/records/20260331-session-routing-removal/08-runtime-developer-findings.md` (Top Finding 1).

`[S][LIB]` — **Role-guidance precision follow-up: proposal-state-claim verification and update-report filename compliance** — Add two reusable role-template clarifications. `$GENERAL_CURATOR_ROLE`: when a brief makes a specific claim about the current state of a source document, verify the cited document during proposal preparation and flag unverifiable claims explicitly. `$GENERAL_OWNER_ROLE`: when a closure instruction specifies an update-report publish path, use the update-report filename contract `[YYYY-MM-DD]-[brief-descriptor].md` within the project's updates directory rather than a date-only filename. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$GENERAL_CURATOR_ROLE` proposal-stage source-claim verification or `$GENERAL_OWNER_ROLE` closure-time update-report filename precision; the prior role-guidance precision bundle is closed, so no active merge target exists. Source: `a-society/a-docs/records/20260403-role-guidance-precision-bundle/06-curator-findings.md` (Finding 1; Missing Information; Finding 2; Workflow Friction).

---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
