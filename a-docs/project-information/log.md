# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` — Small — single-session maintenance change (one to two files) |
| `[M]` — Medium — multi-step change within a single phase |
| `[L]` — Large — multi-phase or multi-file strategic change |
| `[LIB]` — Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` — Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` — Direction decision involved; requires Owner-level scope |
| `[RUNTIME]` — Runtime-root executable change; follows Executable Dev workflow when implementation is required |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v33.0. Standing executable model fully consolidated: `runtime/` is the sole executable root, the former `tooling/` implementation root has been retired, tracked `runtime/dist/` output has been removed, and machine-readable handoff validation is already covered by the existing runtime parser/test surface.

**Recent Focus**

`[S][LIB]` — **backward-pass-findings-template-alignment** (2026-04-10): Aligned the backward-pass findings template with standing meta-analysis instructions. `$GENERAL_IMPROVEMENT_TEMPLATE_FINDINGS` — added `### Role File Gaps` subsection (7th reflection category), `## a-docs Structure Check Notes` section with eight explicit checks, and `## Template Maintenance` footer establishing the standing alignment rule. No version bump; additive template change only. Closes: "Backward-pass findings template alignment for standing analysis families" Next Priority. Record: `a-society/a-docs/records/20260410-backward-pass-findings-template-alignment/`.

**Previous**

`[L][LIB][ADR][RUNTIME]` — **executable-layer-unification-setup** (2026-04-09): Unified the standing executable model. `$A_SOCIETY_VISION`, `$A_SOCIETY_STRUCTURE`, and `$A_SOCIETY_ARCHITECTURE` now treat `runtime/` as the standing executable root and `tooling/` as transitional legacy implementation state only. Created the executable standing-doc set (`$A_SOCIETY_EXECUTABLE`, `$A_SOCIETY_EXECUTABLE_PROPOSAL`, `$A_SOCIETY_EXECUTABLE_ADDENDUM`, `$A_SOCIETY_EXECUTABLE_COUPLING_MAP`, `$A_SOCIETY_EXECUTABLE_LEGACY_TA_ASSESSMENT_PHASE1_2`), the executable workflow (`$A_SOCIETY_WORKFLOW_EXECUTABLE_DEV`), and the new executable role pair (`$A_SOCIETY_FRAMEWORK_SERVICES_DEVELOPER_ROLE`, `$A_SOCIETY_ORCHESTRATION_DEVELOPER_ROLE`); retired the standalone tooling/runtime split in standing docs, indexes, and reusable instruction wording; kept `runtime/INVOCATION.md` as the sole default operator-facing executable reference; published framework update report `a-society/updates/2026-04-09-executable-layer-unification.md`; version bumped v32.1 → v33.0 (Breaking ×3). Record: `a-society/a-docs/records/20260409-executable-layer-unification-setup/`.

`[M][LIB]` — **role-guidance-addenda** (2026-04-08): Completed the reusable role-guidance follow-up bundle across `$GENERAL_TA_ROLE`, `$GENERAL_CURATOR_ROLE`, and `$GENERAL_OWNER_ROLE`. `$GENERAL_TA_ROLE` — `### Specification Rigor` extended from 7 to 12 items with five new advisory-completeness standards: brief constraint evaluation, current-state spot-checking, schema identifier names as contract terms, instrumentation test scope declaration, and call-site enumeration before instrumentation boundaries. `$GENERAL_CURATOR_ROLE` — one new Hard Rule (procedural uncertainty requires escalation-first clarification) and two new Implementation Practices (source-claim verification during proposal preparation; tool-surface terminology normalization during implementation). `$GENERAL_OWNER_ROLE` — ten new `## Brief-Writing Quality` rules (preferred-option pattern, principle/application consistency scan, remove-vs-replace precision, all-occurrences reference-removal scope, update-report path naming, standing-artifact propagation, multi-mode scope declaration, verification-content precision, TA brief constraint/preference partition, stale-description sweep) and three new `## Forward Pass Closure Discipline` rules (accepted residual exception labeling, sequence-slot numbering, archive-target discipline). Framework update report published (`a-society/updates/2026-04-08-role-guidance-addenda.md`); version bumped v32.0 → v32.1 (Recommended ×3). Closes: "Technical Architect advisory completeness addendum" and "Role-guidance precision follow-up: proposal-state claims and closure-time artifact precision". Record: `a-society/a-docs/records/20260408-role-guidance-addenda/`.

`[M][RUNTIME]` — **runtime-observability-foundation** (2026-04-08): Established the OpenTelemetry-based observability foundation for the A-Society runtime. `runtime/src/observability.ts` — new telemetry substrate with OTLP export, payload-capture controls, and SDK lifecycle management. Instrumented `orchestrator.ts` (flow/node context), `orient.ts` (interactive/autonomous turn spans), `llm.ts` (provider spans/events), `handoff.ts` (parsing spans), `triggers.ts` (eval spans), and `improvement.ts` (backward-pass orchestration spans). Added runtime metrics including `a_society.session.turn.started`, `a_society.session.turn.duration`, `a_society.flow.started`, and `a_society.provider.latency`. `runtime/INVOCATION.md` — added "Telemetry and Observability" section documenting `A_SOCIETY_TELEMETRY_ENABLED`, `A_SOCIETY_OTLP_ENDPOINT`, `A_SOCIETY_OTLP_HEADERS`, `A_SOCIETY_TELEMETRY_PAYLOAD_CAPTURE`, and local Jaeger setup. Both indexes updated with expanded `$A_SOCIETY_RUNTIME_INVOCATION` description. No `general/` changes; no update report. Record: `a-society/a-docs/records/20260408-runtime-observability-foundation/`.

---

## Next Priorities

`[S][LIB]` — **Structural-rewrite propagation and standing-log verification guidance** — Add reusable guidance so standing rename / layer-retirement flows do not stop at primary files. Scope: in `$GENERAL_CURATOR_IMPL_PRACTICES`, require proposal-stage propagation mapping and implementation-stage retired-term sweeps across still-live support docs, guide entries, operator references, examples, and tests when a standing role, workflow, layer, or executable surface is renamed or retired; in `$GENERAL_OWNER_LOG_MANAGEMENT` and `$GENERAL_OWNER_CLOSURE`, require closure-time verification of the project log's own taxonomy and current-state wording when framework vocabulary changes. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets structural-rewrite propagation, retired-term sweep guidance, or standing-log taxonomy verification across the reusable Curator/Owner support docs; no merge. Source: `a-society/a-docs/records/20260409-executable-layer-unification-setup/09-curator-findings.md` (Workflow Friction; Role File Gaps; Generalizable Findings); `a-society/a-docs/records/20260409-executable-layer-unification-setup/10-owner-findings.md` (Workflow Friction; Role File Gaps; Top Findings 1, 3, and 4; Framework Contribution Candidates 1 and 3).

`[M]` — **Runtime integration test infrastructure** — The runtime-root executable surface still lacks a reusable harness and format standard for broad integration validation. The `runtime-observability-foundation` flow partially closed this gap by adding a deterministic telemetry test seam (`runtime/test/telemetry-test-helper.ts`) and production-path observability tests covering gateway tool rounds, prompt-human suspension, trigger execution, and improvement closure. Remaining scope: create structured conversational-flow test utilities, including SSE stream mock helpers and lightweight project-fixture builders for registry/index traversal; standardize a lightweight OTLP mock sink or equivalent unified telemetry-capture utility so developers can assert on traces and metrics with execution evidence rather than ad-hoc procedures; and define a reproducible integration-record format that exercises real guard paths beyond observability-focused span assertions. Broader improvement-phase validation should still extend past the current closure-path smoke coverage. Structural setup flow `20260409-executable-layer-unification-setup` resolved the execution boundary: this follow-through now routes through Executable Dev workflow under the Orchestration Developer track. Merge assessment: updated by synthesis from `20260403-interactive-owner-session-handoff-routing`, `20260404-project-scoped-improvement-instructions`, `20260404-runtime-state-isolation`, and `20260408-runtime-observability-foundation` because the new SSE-mocking, fixture-verbosity, improvement-phase coverage, env-var cleanup, OTLP-mock-sink, and telemetry-test-seam findings target the same runtime integration-testing design area, compatible authority level, and the same executable-orchestration implementation path. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/11-runtime-developer-findings.md` (Top Findings 2-3; Missing Information; Workflow Friction); `a-society/a-docs/records/20260403-interactive-owner-session-handoff-routing/12-ta-findings.md` (Top Finding 2; Missing Information); `a-society/a-docs/records/20260404-project-scoped-improvement-instructions/07b-runtime-developer-finding.md` (Top Finding 2; Workflow Friction); `a-society/a-docs/records/20260404-runtime-state-isolation/05-owner-integration-gate.md` (Observation; env-var cleanup); `a-society/a-docs/records/20260408-runtime-observability-foundation/14-ta-integration-review-3.md` (Mandate Corrections C1 and C4; New Observation O1); `a-society/a-docs/records/20260408-runtime-observability-foundation/18-curator-findings.md` (Generalizable Findings); `a-society/a-docs/records/20260408-runtime-observability-foundation/19-runtime-developer-findings.md` (Finding 3).

`[S][LIB][ADR]` — **General agents entry-point variable** — Evaluate adding a project-agnostic `$GENERAL_AGENTS` variable to `$A_SOCIETY_INDEX` to simplify generic instruction-text references to a project's agents entry-point. Requires Owner direction on what the variable should point to (a new ready-made general agents.md template, an existing path, or a different approach) and whether a corresponding file in `general/` is warranted. Follows Framework Dev workflow. Merge assessment: no existing Next Priorities item targets `$A_SOCIETY_INDEX` in this design area; no merge. Source: `a-society/a-docs/records/20260329-owner-protocol-and-role-guidance-bundle/06-curator-findings.md` (Top Finding 3).

`[S][LIB]` — **Owner integration-gate review and Curator registration-boundary guidance** — Add reusable role-template guidance for two recurring execution boundaries. `$GENERAL_OWNER_ROLE`: when the Owner reviews a TA-reviewed integration result, define the evidence hierarchy (approved design > live implementation/operator reference > TA recommendation), the blocking status of schema-name drift, the boundary on "documented deviations," the requirement to distinguish production-path evidence from synthetic tests, and the Owner's authority to override a TA approval recommendation based on direct source comparison. `$GENERAL_CURATOR_ROLE`: during registration, prohibit mutation of closure-owned log lifecycle sections (`Recent Focus`, `Previous`, archive) and require registration artifacts to report needed closure-time log updates rather than performing them. Follows Framework Dev workflow. Merge assessment: existing "Role-guidance precision follow-up" targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`, compatible authority and workflow, but its open scope is proposal-state and closure-time precision rather than integration-gate review or registration-boundary behavior, so no merge. Source: `a-society/a-docs/records/20260408-runtime-observability-foundation/18-curator-findings.md` (Top Finding 1; Workflow Friction); `a-society/a-docs/records/20260408-runtime-observability-foundation/20-technical-architect-findings.md` (Top Findings 1-2); `a-society/a-docs/records/20260408-runtime-observability-foundation/21-owner-findings.md` (Top Findings 1 and 3; Framework Contribution Candidates 1 and 3).

`[S]` — **Public/internal index parity validation** — Extend the executable path-validation capability, or a companion executable check, so duplicated variables that appear in both `$A_SOCIETY_INDEX` and `$A_SOCIETY_PUBLIC_INDEX` are compared automatically instead of relying on Curator-side manual parity checks. Scope: define which shared variables require parity validation, compare both path and description fields, and report mismatches in a review-friendly form. Follows Executable Dev workflow. Merge assessment: no existing Next Priorities item targets cross-index parity validation or duplicated-variable synchronization between the internal and public indexes; no merge. Source: `a-society/a-docs/records/20260408-runtime-observability-foundation/18-curator-findings.md` (Workflow Friction; Top Finding 3).

`[L][ADR][RUNTIME]` — **Runtime operator-surface event model and parallel-track visibility** — Define a first-class operator/system event surface for the runtime so model output, tool activity, and runtime state changes are visually distinct and understandable. Scope: add explicit system notices for handoff success, handoff repair/retry, active-role activation, forward-pass closure, and token-status availability; ensure newline separation between assistant output and runtime notices; and define how concurrently active nodes or future parallel-track work should be surfaced to the operator, including whether the CLI is sufficient or a richer UI is warranted. Follows Executable Dev workflow with operator-surface design work up front. Merge assessment: no existing Next Priorities item targets runtime operator event surfacing, repair-loop transparency, or parallel-track operator visibility; no merge. Source: human-observed interactive Framework Dev flow transcript for `20260410-backward-pass-findings-template-alignment` plus post-flow operator review (hidden repair loop, ambiguous handoff status, missing success/role-active notices, missing token fallback notice, and no clear model for future parallel output).

`[M][ADR][RUNTIME]` — **Startup context-read timing and same-role session continuity** — Reduce redundant rereads and wrong-time support-doc loads caused by the current startup prompt and node-scoped session model. Scope: treat injected required-reading files as already loaded at startup, revise the initial Owner prompt and any startup role wording that encourages rereading already-injected authority files, and decide whether same-role returns within one flow should reuse a role-scoped continuity layer or receive an explicit carried-forward summary instead of restarting from fresh node-scoped context. Follows the multi-domain development pattern (Framework Dev plus Executable Dev) because the fix spans runtime prompting/session behavior and A-Society role/workflow guidance. Merge assessment: no existing Next Priorities item targets injected-context read timing, startup prompt wording, eager support-doc read cues, or same-role session continuity behavior; no merge. Source: human-observed interactive Framework Dev flow transcript for `20260410-backward-pass-findings-template-alignment` plus post-flow operator review (redundant log/workflow rereads and out-of-time log-management loading).

`[S][RUNTIME]` — **Runtime schema/repair prompt single-source alignment** — Eliminate drift between the runtime's live workflow/handoff validators and the repair guidance it feeds back to the model. Scope: derive repair prompts from the same live schema or validator contract used at runtime, remove stale hardcoded examples (including workflow-node fields that are no longer required), and make typed-signal validation outcomes explicit enough that the operator and model can distinguish a malformed handoff from an unsupported signal. Follows Executable Dev workflow. Merge assessment: no existing Next Priorities item targets runtime schema-message drift, stale repair examples, or single-source validation guidance for workflow/handoff contracts; no merge. Source: human-observed interactive Framework Dev flow transcript for `20260410-backward-pass-findings-template-alignment` plus post-flow operator review (`workflow.md` repair guidance still referenced `description`, and terminal-signal failure mode was not clearly surfaced).

---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
