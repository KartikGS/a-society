# A-Society: Project Log

## Scope Tags

| Tag | Meaning |
|---|---|
| `[S]` | Small — single-session maintenance change (one to two files) |
| `[M]` | Medium — multi-step change within a single phase |
| `[L]` | Large — multi-phase or multi-file strategic change |
| `[LIB]` | Library — creates or modifies `general/` content; requires Owner approval; may require a framework update report |
| `[MAINT]` | Maintenance — a-docs change within Curator authority; no Owner proposal needed |
| `[ADR]` | Direction decision involved; requires Owner-level scope |
| `[RUNTIME]` | Runtime layer change; follows Runtime Dev workflow |

Tags may be combined: `[M][LIB]`, `[S][MAINT]`, `[L][LIB][ADR]`.

---

## Entry Lifecycle

- **Recent Focus** — the most recently completed flow. Exactly one entry.
- **Previous** — up to three prior completed flows.
- When a fourth `Previous` entry would be added, the oldest moves to `$A_SOCIETY_LOG_ARCHIVE`.
- Entries are added when a flow closes (Owner signs off, or Curator completes Curator-authority work). Entries are not modified after archival.

---

## Current State

**Status:** Framework active at v24.1.

**Recent Focus**

`[M][LIB]` — **workflow-schema-unification** (2026-03-29): Framework Dev flow closed (Phase 1 of 3 in multi-flow schema unification). Four items: *(1)* `$A_SOCIETY_RECORDS` — workflow.md schema updated to nodes/edges format; completeness obligation updated to node/edge terminology; "What Component 4 reads" and "Relationship to plan's path" passages updated; *(2)* `$INSTRUCTION_RECORDS` — same schema update; `synthesis_role` removed; cross-reference to `$INSTRUCTION_WORKFLOW_GRAPH` added for schema definition; completeness obligation, Step 2, and plan relationship passages updated; *(3)* `$INSTRUCTION_WORKFLOW_GRAPH` — new "Record-Folder workflow.md — Subgraph Variant" section appended; *(4)* `$A_SOCIETY_TOOLING_COUPLING_MAP` — Component 4 row updated to nodes/edges schema with Tooling Dev pending note. Framework update report published (Recommended, v24.0 → v24.1). Backward pass complete (Curator synthesis, 2026-03-29). Synthesis MAINT: `$A_SOCIETY_OWNER_ROLE` now requires brief-time mirror assessment when project-specific conventions instantiate general instructions and requires schema-migration briefs to scope a vocabulary sweep; `$A_SOCIETY_CURATOR_ROLE` now requires an implementation-stage terminology sweep for schema changes. Record: `a-society/a-docs/records/20260329-workflow-schema-unification/`.

**Previous**

`[M]` — **runtime-env-bundle** (2026-03-28): Runtime Dev flow closed. Four items bundled: *(1)* dotenv support — `dotenv: ^16.0.0` added to `runtime/package.json`; `import 'dotenv/config'` added as first statement in `cli.ts`; `runtime/.env.sample` created documenting all seven env vars (`LLM_PROVIDER`, `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`, `OPENAI_COMPAT_BASE_URL`, `OPENAI_COMPAT_API_KEY`, `OPENAI_COMPAT_MODEL`, `SYNTHESIS_ROLE`); `.env` added to `runtime/.gitignore`; *(2)* provider catch-block fix — `anthropic.ts` and `openai-compatible.ts` outer catch blocks now re-throw pre-classified `LLMGatewayError` before SDK-specific mapping, preserving original error classification; *(3)* `synthesisRole` parameterized in `triggers.ts` — `TERMINAL_FORWARD_PASS` branch reads `process.env.SYNTHESIS_ROLE ?? 'Curator'` and includes resolved role in trigger record `resultSummary`; *(4)* `runtime/INVOCATION.md` updated — `orient` command, provider environment variable table, and `.env` file usage sections added; three pre-existing command-signature inaccuracies corrected (`start-flow` missing `<projectRoot>`, `resume-flow` wrong args, `flow-status` spurious arg). No `general/` changes; no framework update report. Backward pass complete (Curator synthesis, 2026-03-29). Synthesis MAINT: `$A_SOCIETY_INDEX` now registers `$A_SOCIETY_RUNTIME_INVOCATION`; `$A_SOCIETY_WORKFLOW_RUNTIME_DEV` now requires documentation-accuracy checks at TA review, Owner gate, and Curator registration; `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE` now requires directory-scoped file-existence verification and operator-facing reference verification during integration review; `$A_SOCIETY_RUNTIME_DEVELOPER_ROLE` now preserves pre-classified gateway errors in provider adapters and keeps one-shot diagnostics out of the runtime root. Record: `a-society/a-docs/records/20260328-runtime-env-bundle/`.

`[M][LIB][MAINT]` — **general-doc-bundle** (2026-03-28): Framework Dev flow closed. Six `general/` improvements bundled — *(1)* `$GENERAL_IMPROVEMENT` Meta-Analysis Phase Step 2: sequence-number verification sentence added; *(2)* `$GENERAL_OWNER_ROLE` ×3 insertions + 2 new sections + `$GENERAL_CURATOR_ROLE` ×3 insertions: shared list search, expected-response scope, `[LIB]` registration clarity, update-report TBD form, registration scope, technical-summary discipline, TA Advisory Review, and Tooling Invocation Discipline; *(3)* `$INSTRUCTION_RECORDS`: owner decision naming forward-pass-only distinction; *(4)* `$INSTRUCTION_MACHINE_READABLE_HANDOFF` ×3: phase-closure `artifact_path` semantics, phase-closure worked example, synthesis/flow-closure note; *(5)* new `general/roles/technical-architect.md` — four generalizable advisory standards (§8 completeness, extension-before-bypass code, extension-before-bypass dependencies, malformed-input state), registered in `$GENERAL_MANIFEST`, `$A_SOCIETY_PUBLIC_INDEX`, and `$A_SOCIETY_INDEX`; *(6)* `$INSTRUCTION_DEVELOPMENT` ×2: Completion Report Requirements and Integration Test Record Format sections appended. Framework update report published (10 Recommended, 3 Optional, v23.0 → v23.1). Backward pass complete (Curator synthesis, 2026-03-28). Synthesis MAINT: `$A_SOCIETY_CURATOR_ROLE` now requires proposal-stage rendered-content matching against adjacent target-file exemplars; `$A_SOCIETY_OWNER_ROLE` prose-insertion guidance now requires immediately adjacent boundary anchors, and a new Constraint-Writing Quality section scopes registration checks by newly created or modified files rather than parent directories. Record: `a-society/a-docs/records/20260328-general-doc-bundle/`.

`[S][LIB][MAINT]` — **improvement-synthesis-third-criterion** (2026-03-28): Framework Dev flow closed. Third merge criterion added to Synthesis Phase of both improvement documents: *(1)* `general/improvement/main.md` — Synthesis Phase merge assessment updated to three-criterion numbered list — "(1) same target files or same design area, (2) compatible authority level, and (3) same workflow type and role path"; *(2)* `a-society/a-docs/improvement/main.md` — Synthesis Phase: same update. Sync fix aligning both documents with `$A_SOCIETY_OWNER_ROLE` and `$INSTRUCTION_LOG` (updated in doc-maintenance-bundle, 2026-03-24, not propagated). Framework update report published (Breaking, v22.1 → v23.0): adopting projects with instantiated improvement documents must update their Synthesis Phase merge assessment to add criterion (3). Backward pass complete (Curator synthesis, 2026-03-28). Synthesis MAINT: `$A_SOCIETY_RECORDS` Owner decision naming clarified to apply to forward-pass actions only; backward-pass work does not turn a terminal closure artifact into `owner-to-[role]`; `$A_SOCIETY_OWNER_ROLE` `[LIB]` brief trigger clarified that pre-classification update report drafts use `TBD` classification fields pending Phase 4 assessment via `$A_SOCIETY_UPDATES_PROTOCOL`. Record: `a-society/a-docs/records/20260328-improvement-synthesis-third-criterion/`.

---

## Next Priorities

`[M][LIB]` — **Role guidance precision bundle** — Add eight generalizable role-guidance clarifications: *(1)* `$GENERAL_CURATOR_ROLE` should require proposal-stage rendered-content matching against adjacent target-file exemplars when proposing formatted blocks; *(2)* `$GENERAL_OWNER_ROLE` prose-insertion guidance should require immediately adjacent boundary anchors, not nearby landmarks within a broader range; *(3)* `$GENERAL_OWNER_ROLE` should add constraint-writing quality guidance requiring registration/index constraints to scope by newly created or modified files rather than parent directories; *(4)* `$GENERAL_OWNER_ROLE` should require that proposed instruction text use real registered `$VAR` names or a functional description when no project-agnostic variable exists, not fictional placeholder variables; *(5)* `$GENERAL_OWNER_ROLE` forward-pass closure guidance should state that any Next Priorities items surfaced by the closing flow are written to the log before the closure artifact is filed; *(6)* `$GENERAL_OWNER_ROLE` should require explicit mirror assessment when a brief modifies a project-specific convention that instantiates a general instruction, either scoping the general counterpart as a co-change or declaring it out of scope with rationale; *(7)* `$GENERAL_OWNER_ROLE` should require schema-migration briefs to scope a vocabulary sweep for deprecated terminology in surrounding prose; *(8)* `$GENERAL_CURATOR_ROLE` should require an implementation-stage terminology sweep when schema changes rename structural terms. Follows Framework Dev workflow. Merge assessment: originally new item because no existing Next Priorities item targeted `$GENERAL_OWNER_ROLE`, `$GENERAL_CURATOR_ROLE`, or this role-guidance precision area; updated by synthesis from `20260329-agent-context-frontmatter` because the new findings target the same file (`$GENERAL_OWNER_ROLE`), the same role-guidance precision design area, compatible `[LIB]` authority, and the same Framework Dev workflow path; updated again by synthesis from `20260329-workflow-schema-unification` because the new findings target the same files (`$GENERAL_OWNER_ROLE`, `$GENERAL_CURATOR_ROLE`), the same role-guidance precision design area, compatible `[LIB]` authority, and the same Framework Dev workflow path. Source: `a-society/a-docs/records/20260328-general-doc-bundle/06-curator-findings.md` (Findings 1-2); `a-society/a-docs/records/20260328-general-doc-bundle/07-owner-findings.md` (Findings 1-2); `a-society/a-docs/records/20260329-agent-context-frontmatter/09-curator-findings.md` (Top Findings 1-2); `a-society/a-docs/records/20260329-agent-context-frontmatter/10-owner-findings.md` (Top Findings 1-2); `a-society/a-docs/records/20260329-workflow-schema-unification/07-curator-findings.md` (Proposed Improvements 1, 3); `a-society/a-docs/records/20260329-workflow-schema-unification/08-owner-findings.md` (Top Findings 1-2).

`[S][LIB]` — **Technical Architect verification standards addendum** — Add a generalizable TA advisory standard requiring directory-scoped verification before asserting that a file exists or does not exist. This was implemented directly in `$A_SOCIETY_TECHNICAL_ARCHITECT_ROLE`; the reusable counterpart remains open for `$GENERAL_TA_ROLE`. Follows Framework Dev workflow. Merge assessment: existing "Role guidance precision bundle" targets `$GENERAL_OWNER_ROLE` and `$GENERAL_CURATOR_ROLE`; compatible authority level and workflow type, but different target file and TA-advisory verification design area, so no merge. Source: `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 1).

`[M]` — **Machine-readable handoff validator (Component 8)** — New tooling component: extracts the `handoff` fenced block from agent pause-point output, parses the YAML, and validates field types, enum constraints, and the `prompt` conditional against `session_action`. Structurally analogous to Component 7 (Plan Artifact Validator) — same deterministic, rule-derived pattern, different input source. Follows standard Tooling Dev flow. Source: `a-society/a-docs/records/20260325-machine-readable-handoff/03-curator-to-owner.md` (Tooling Validator Assessment).

`[M]` — **Workflow schema unification — Tooling Dev + Runtime Dev phases** — Framework Dev phase complete (2026-03-29; record: `a-society/a-docs/records/20260329-workflow-schema-unification/`; v24.0 → v24.1). Two flows remaining in sequence: *(1)* Tooling Dev flow — Component 4 parser updated to read nodes/edges instead of path[]; coupling map "Tooling Dev pending" note resolved; *(2)* Runtime Dev flow — orchestrator reads `workflow.md` from record folder; `start-flow` CLI signature updated (`workflowDocumentPath` arg dropped; graph derived from `recordFolderPath/workflow.md`); `$A_SOCIETY_RUNTIME_INVOCATION` updated. Merge assessment: "Workflow-derived synthesis role routing" targets different design area; no merge. Source: `a-society/a-docs/records/20260329-workflow-schema-unification/`.

`[S][MAINT]` — **Add Component 7 to `$A_SOCIETY_TOOLING_INVOCATION`** — `tooling/INVOCATION.md` documents Components 1–6 but omits Component 7 (`plan-artifact-validator.ts`). Entry point: `validatePlanArtifact(recordFolderPath: string)`. Type B coupling-map gap; should have shipped with Component 7. Merge assessment: no existing Next Priorities item targets INVOCATION.md. New item. Source: `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/15-runtime-developer-findings.md` (Finding 1); `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/19-ta-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orchestrator-mvp/20-owner-findings.md` (Finding 4).

`[M]` — **Runtime integration test infrastructure** — The runtime layer has no test harness or format standard for integration validation. The integration gate requires a reproducible test record; the Developer improvised a manual terminal transcript that could not exercise the registry guard path (mock API key short-circuited before that path ran). A structured test harness would have surfaced the missing guard at validation time rather than in the backward pass. Follows Runtime Dev workflow. Source: `a-society/a-docs/records/20260327-runtime-orient-session/15-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260327-runtime-orient-session/13-runtime-developer-findings.md` (Finding 2).

`[S][RUNTIME]` — **Replace static `roleContextRegistry` with dynamic frontmatter reader** — Replace the hardcoded `roleContextRegistry` object in `runtime/src/registry.ts` with a function that reads `universal_required_reading` from the project's `agents.md` and `required_reading` from the role file at session start, using `resolveVariableFromIndex()` in `paths.ts` to resolve `$VAR` references to file paths. This eliminates the remaining co-maintenance gap between `registry.ts` and role documentation now that YAML frontmatter is the single source of truth. Runtime Dev workflow only — no framework doc or tooling changes required. Merge assessment: no existing Next Priorities item targets `registry.ts`. "Workflow schema unification" has a Runtime Dev component (orchestrator.ts, CLI) — different file and design area; no merge. Source: `a-society/a-docs/records/20260329-agent-context-frontmatter/` (Cluster B follow-on).

`[M][LIB][ADR]` — **Workflow-derived synthesis role routing** — Revisit the runtime's `synthesisRole` source of truth. The current env-var approach works for A-Society's current flows but couples routing to deployment configuration; the long-term correct home is the workflow artifact layer rather than the host environment. Expected scope: Runtime Dev design reconsideration plus any required workflow-graph schema and instruction updates. Follows Runtime Dev workflow with TA advisory and Owner gate; may require Framework Dev follow-on if the workflow-graph schema changes. Merge assessment: existing runtime Next Priorities items target test infrastructure, invocation registration, or `a-docs/` dependency tracking; different files and design area, so no merge. Source: `a-society/a-docs/records/20260328-runtime-env-bundle/12-runtime-developer-findings.md` (Finding 2); `a-society/a-docs/records/20260328-runtime-env-bundle/13-owner-findings.md` (Finding 2); `a-society/a-docs/records/20260328-runtime-env-bundle/14-ta-findings.md` (Finding 3).

---

Archived flows are recorded in `$A_SOCIETY_LOG_ARCHIVE`. One entry per flow. Entries are immutable once written. Most recent at top.
